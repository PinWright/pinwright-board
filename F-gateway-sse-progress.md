---
id: F-gateway-sse-progress
title: "Gateway: opt-in SSE responses with job progress notifications on POST /mcp"
status: IN-REVIEW
severity: Medium
category: feature
tags: [mcp, transport, sse, progress, jobs, parity-ue58]
---

# Gateway: opt-in SSE responses with job progress notifications on POST /mcp

The C++ gateway (`Transport\McpTransport.cpp`) is strictly request-response: it ignores `Accept`, always returns `application/json`, and long-running work is poll-only via `system.job_status`. Client research (July 2026) confirmed SSE is a strict superset, not a second protocol: the Streamable HTTP spec (rev 2025-03-26/2025-06-18, "Sending Messages to the Server") REQUIRES every conformant client to accept both `application/json` and `text/event-stream` on one endpoint, so plain JSON remains the default and nobody is locked out. Claude Code, Codex, Cursor 1.0+, Gemini CLI (httpUrl), VS Code 1.101+, Windsurf all speak streamable HTTP; Claude Desktop stays on the bundled stdio proxy.

Primary payoff: Claude Code renders `notifications/progress` in its tool UI AND progress resets its 5-minute idle abort (v2.1.187+), so long editor jobs (UBT runs, dump_folder sweeps, test runs) stop dying at the client timeout. Codex/Cursor/Gemini currently drop the notifications (legal, inert).

Scope constraints (from research; keep all):
- Upgrade a response to SSE ONLY when the request carries `_meta.progressToken` AND `Accept` includes `text/event-stream`; otherwise return `application/json` exactly as today.
- Stay stateless: never issue `Mcp-Session-Id` (immunity to the -32000 stale-session failure class on editor restarts).
- `GET /mcp` returns 405. No JSON-RPC batch. Bundled stdio proxy unaffected via Accept gating.
- Wire job progress events (the `Ctx.StartJob` ticket stream / jobs.jsonl source) into `notifications/progress` for the streaming request; final result terminates the stream.
- Epic parity note: UE 5.8's built-in server is SSE-always with a heartbeat-only counter; ours should send REAL job progress.

NOT in scope (user decision): sessions, `tools/list_changed`, MCP resources. (These are orthogonal to SSE: sessions are an optional spec feature, list_changed needs a standing GET stream and is useless for a single never-changing `call` tool, resources are plain request/response.)

Engine constraint (verified 2026-07-05): incremental response writes (`EHttpServerResponseFlags::MultipleWriteStream`/`HasAdditionalWrites`/`SkipHeaderWrite`, Runtime\Online\HTTPServer) are NEW in UE 5.8 and absent in 5.6/5.7 engine source. **Version-gate the SSE path to 5.8+**; 5.3-5.7 builds keep plain JSON + job polling unchanged. Do not build a custom socket listener for older engines. Epic's ModelContextProtocolServer.cpp is the working reference, including the synchronous write-callback reentry contract (HttpResultCallback.h) and mid-stream disconnect teardown (HttpConnection.cpp:183).

Job-system interplay (design decisions):
- Job tickets remain the canonical handle; SSE is a live view. First progress event on a stream must carry the job ticket id so a dropped stream degrades to polling.
- Do NOT infer block-and-stream from progressToken alone: Codex's rmcp auto-injects a progressToken on every call, which would flip all job RPCs to blocking for that client. Streaming a job to completion requires an explicit per-call opt-in (e.g. `wait: true`) in addition to token + Accept.

Acceptance: a long `system.run_tests` call from Claude Code with a progressToken streams progress and survives past 5 minutes; the same call from a plain-JSON client (no Accept: text/event-stream) behaves byte-identically to today.

## History
- `#1-sse-strict-superset` `OPEN` reporter — Gateway is JSON-only + poll-only; research confirms SSE via Accept+progressToken gating is a spec-mandated superset (no client lock-out) and Claude Code both renders progress and resets its 5-min idle abort on it. Add opt-in SSE with real job progress; stay stateless.
- `#2-version-gate-and-job-interplay` `OPEN` reporter — Verified engine streaming flags (MultipleWriteStream) are new in UE 5.8 and absent in 5.6/5.7: version-gate SSE to 5.8+, older engines unchanged. Added design constraints: job tickets stay canonical (stream embeds ticket id), and streaming a job to completion needs explicit per-call opt-in because Codex auto-injects progressToken on every call.
- `#3-custom-socket-transport-landed` `OPEN` developer — Implementation landed, pending verification; status stays OPEN and the developer's OPEN→IN-REVIEW transition is deliberately deferred until the Wave-2 test run is green. The `#2` version-gate decision was REVERSED by the user: SSE is NOT gated to UE 5.8's MultipleWriteStream flags (would leave 5.3-5.7 without streaming; engine-source patching stays forbidden) — instead a NEW custom socket HTTP/1.1 transport (`Transport/SocketHttpServer.{h,cpp}`, non-blocking single I/O thread) serves the streaming path uniformly on UE 5.3-5.8, superseding `#2`'s "do not build a custom socket listener" line. Triple gate implemented as specced: streams iff `_meta.progressToken` + `Accept: text/event-stream` + explicit tool-call `args.wait == true` (the wait flag neutralizes Codex's auto-injected progressToken); any other combination is byte-identical plain JSON. Stateless preserved (no Mcp-Session-Id; `GET /mcp` 405, other GETs 404; no batch; Content-Type on every response). Progress frames always carry `ticket_id` (tickets stay canonical; dropped stream degrades to `system.job_status` polling; jobs.jsonl unchanged); heartbeat `: ping` frames every SseHeartbeatSeconds (default 15); big jobs (run_ubt, run_tests, pipeline.run_ubt, level builds) now emit incremental RecordProgress. Shared request logic extracted to `Transport/McpRequestCore.{h,cpp}` used by both transports; selection via new setting `bEnableStreamingTransport` (default false; once the all-client smoke matrix passes, the default flips and the legacy IHttpRouter transport is DELETED — one code path end state). Bundled stdio proxy (`mcp_proxy.py`) streams too, forwarding progress notifications to stdout when the call opts in.
- `#4-wave2-green` `IN-REVIEW` developer — Wave-2 verification complete on UE 5.6; moving OPEN→IN-REVIEW per the deferred transition in `#3`. Compile green (PDSEditor Development). New `Tests/Transport/` suite: 26/26 pass, plus 3 consecutive re-runs to prove stability; proxy SSE parser 8/8 (bundled Python unittest). Full plugin suite: 3619 found / 3616 pass; the 3 fails are pre-existing EAContentExamples57 fixture tests (`physics.setup_physics_simulation` + `skeleton.create_physics_asset` MeshBackedCreatesBodiesHeadless, `Sequencer.ControlRigTrack.AddKeyReadback` — SKM_Manny fixture absent in the PDS host; fail identically without this change). Three real defects found and fixed during the loop: (1) inverted FSocketBSD::Recv would-block/FIN semantics on both server poll and test client; (2) TCP RST from immediate close-with-unread-inbound destroying already-sent early-reject responses — fixed with FIN-then-drain lingering close; (3) inbound flood backstop bypassing the linger on doomed connections (load-dependent RST race) — doomed connections now discard inbound instead of accumulating. Remaining before DONE: all-client smoke matrix (Claude Code, Codex, Cursor, Gemini, VS Code, Windsurf, stdio proxy) against a live editor with `bEnableStreamingTransport=true`, then default flip + legacy IHttpRouter deletion; 5.3-5.8 version matrix via mcp-version-matrix afterwards.
- `#5-legacy-transport-deleted` `IN-REVIEW` developer — USER DECISION: the smoke matrix does NOT gate the cutover — socket transport is now the ONLY transport, always on. Deleted `Transport/McpTransport.{h,cpp}` (FMcpTransport/IHttpRouter path), `Utils/HttpUtils.h`, the `bEnableStreamingTransport` setting, the dispatcher's legacy Initialize overload, and the `HTTPServer` Build.cs dependency; shared delegate types moved to `Transport/McpTransportTypes.h`. Tests migrated: `infra.mcp_transport.*` (20 tests) ported to `infra.request_core.*` against `McpRequestCore::ProcessRequestBody`/`WrapToolResult`; 35 files sink-swapped; `TestGetComponentsLargePayload` ported off live-HTTP transports. Post-deletion verification on UE 5.6: compile green, full suite 3619 found / 3619 reported / 3 fails (the same pre-existing EAContentExamples57 fixture tests from `#4`), socket transport binds as the live subsystem transport. Remaining before DONE: all-client smoke verification (now informational, not a gate) and the 5.3-5.8 mcp-version-matrix. Known small gap: socket suite asserts 401+Content-Type but not `WWW-Authenticate: Bearer` on the wire (the ported request_core test dropped that wire-level sub-assertion).
- `#6-www-authenticate-assertion-added` `IN-REVIEW` developer -- Closed the coverage gap from `#5`: both 401 tests in Tests/Transport/TestSocketHttpParse.cpp (AuthMissing401, AuthWrongBearer401) now assert the RFC 7235 `WWW-Authenticate: Bearer` challenge on the wire. Verified: compile green, socket_http.Parse group 10/10 pass (Saved/Logs/PDS_auth_header_check.log).
- `#7-wait-default-flipped` `IN-REVIEW` developer -- USER DECISION: job-RPC streaming flips from opt-in to opt-out, matching UE 5.8's block-by-default model. NEW rule: a request blocks-and-streams (SSE progress frames + terminal result on one call) whenever `params._meta.progressToken` is present AND `Accept: text/event-stream` is sent; the caller opts OUT with tool-call `args: {wait: false}`, which returns the job ticket immediately (fire-and-forget + `system.job_status` polling, unchanged); `wait` absent or true = block-and-stream; plain-JSON clients (no token or no Accept) still always get tickets. Supersedes `#3`'s `wait == true` triple gate. Rationale: agents expect a tool call to return the completed result; progress frames + heartbeats keep client idle-timers alive; Epic's UE 5.8 server is SSE-always with no opt-out at all -- ours keeps the opt-out and the ticket fallback. Residual risk: Codex's `tool_timeout_sec` (300s in our generated config) may not reset on progress notifications -- long jobs from Codex may need `wait: false` + polling; unverified. Tests being updated to the 12-row truth table.
- `#8-wait-flip-live-verified` `IN-REVIEW` developer -- Block-by-default verified end-to-end on UE 5.6: compile green; transport + request_core suites 48/48 (12-row opt-out truth table, inverted Codex case, WaitFalseOptOutBuffered, WaitAccepted core assertions); proxy 14/14. Live through Claude Code -> respawned stdio proxy -> editor: bare `editor.screenshot` (no wait) blocks and returns the final result; `wait:false` returns the running ticket; `asset.dump_folder` blocks to its terminal summary via the job-event bridge. Gotcha for verifiers: the stdio proxy is a long-lived process -- after editing mcp_proxy.py the MCP client must reconnect (respawn) or the old gate keeps running.
- `#9-the-progress-half-was-100-percent-ineffective` `IN-REVIEW` reporter — Status unchanged; this corrects the record rather than the verdict. **Every `notifications/progress` frame this feature ever emitted was discarded by the client.** `HandleJobEvent` sent the server's job ticket id as `progressToken`, but MCP 2025-06-18 admits only tokens "provided in an active request", so a conforming receiver had no request to attribute the frame to. Measured: **83 rejections over five days, 2026-08-12 to 2026-08-16**, every one `Received a progress notification for an unknown token`, each additionally tearing down and reconnecting the stdio transport. The transport had parsed the caller's token all along (`McpRequestCore.cpp:802-809`) and thrown it away, because `FPendingCompletion` had nowhere to put it. **The rejections were already accumulating when `#8` was written.** `#8`'s live verification is not withdrawn — the block-and-stream behaviour it checked (a bare call blocks and returns its terminal result, `wait:false` returns a ticket, `asset.dump_folder` blocks to its terminal summary) is real and unaffected — but every one of those observables is **downstream of the progress frames**, so the verification could not see that the feature was ineffective at the thing it is named for. The same blind spot is in the suite: `Tests/Transport/TestSseFraming.cpp` and `TestStreamLifecycle.cpp` inject the notification they later assert on, round-tripping the framing without ever observing which token the server would choose, and `TestStreamGate.cpp` asserts only that a token's presence upgrades the response — the one part that worked. Fixed and filed as `B-mcp-progress-token-never-echoed` (`30b86d26`, on `origin/master`), which also closed two further conformance defects in the same 25 lines: `progress` fell back to a ring-trimmed array length so a long job counted to 50 and reported 50 forever, and `total` was never sent at all. A tester closing this feature ticket should assert on the token a real client receives, not on framing the test built itself.
