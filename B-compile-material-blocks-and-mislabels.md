---
id: B-compile-material-blocks-and-mislabels
title: "compile_material blocks the game thread for a whole cold shader compile with no job handle, and the stall is reported to every other caller as a retryable EDITOR_NOT_READY — the accurate EDITOR_GAME_THREAD_STALLED result is produced and then discarded by the proxy"
status: IN-REVIEW
severity: High
category: bug
tags: [material, material-authoring, compile_material, game-thread-block, shader-compile, watchdog, EDITOR_NOT_READY, EDITOR_GAME_THREAD_STALLED, job-handle, concurrency, wrong-error-code]
encounters: 1
lastSeen: 2026-08-20T00:00:00Z
---

# A four-minute synchronous block that tells everyone else the editor is still starting up

## The block

`Source/PinWright/Private/Handlers/Material/MaterialAuthoringHandler.cpp:3476` calls
`MaterialCompileErrorCollector::WaitAndCollect`, which is
`Source/PinWright/Private/Handlers/Material/MaterialCompileErrorCollector.h:46-51`:

```cpp
Material->CacheShaders(EMaterialShaderPrecompileMode::Synchronous);   // :46
...
GShaderCompilingManager->FinishAllCompilation();                      // :51
```

Both run on the game thread, unbounded, with no progress, no timeout and no cancel. On a fresh
master's first compile that is minutes — ~4 measured. Before the wait, `:3468`
(`ApplyMasterMaterialEdit`) has already built an `FMaterialUpdateContext` with
`RecreateRenderStates | SyncWithRenderingThread`, which flushes rendering commands in both its
constructor and destructor, plus `UpdateAllComponentMaterialInstances(true)` per consuming landscape.

There is **no job handle**: `grep StartJob` over `Source/PinWright/Private/Handlers/Material/`
returns zero hits; the handler is straight-line to `Ctx.SendSuccess` at `:3531`. And `wait: false`
does not help — `bWaitAccepted` (`Transport/McpRequestCore.cpp:810-816`, consumed at
`Transport/SocketHttpServer.cpp:997-1002`) only selects ticket-vs-SSE for the HTTP response. It frees
the HTTP caller; the handler stays synchronous on the game thread and the block is unchanged.

## The error code is wrong, and the right one is thrown away

Past `GameThreadStallReportSeconds` (default **90.0 s**, `PinWrightSettings.cpp:43`) the I/O-thread
probe sets `bGameThreadStalled` (`Transport/SocketHttpServer.cpp:867-874`). That folds into
`editorReady: false` (`Transport/McpRequestCore.cpp:380-381`), and `:411` emits the dedicated
`EDITOR_GAME_THREAD_STALLED` **with `inFlightMethod`** — precisely the diagnosis a caller needs.

The proxy then discards it. `Content/Python/mcp_proxy.py:1596-1601` (`_probe_state`) checks
`blockedOnModal`, then simply tests `"editorReady" in result` and returns `"not_ready"`. It never
inspects `gameThreadStalled`. `"not_ready"` maps to `EDITOR_NOT_READY` (`:3067`, `:3089`) and is
marked `retryable: True` (`:1699`).

So from t≈90 s every other client is told the editor is **still starting up and worth retrying**,
while it is in fact executing their own previous call — which invites exactly the poll loop that
keeps the watchdog hot. A wrong condition, named confidently, on a normal path.

## Concurrency is refused, not queued

`mcp_proxy.py:3057-3076` probes before forwarding and returns `_editor_unavailable_result(...)` for
any non-`alive` state, so a second compile is rejected outright. The in-editor gate
(`McpRequestCore.cpp:612`) would also refuse, though note it tests
`!IsEditorOperational(Config) || bBlockedOnModal` and does **not** include `bGameThreadStalled`.

## What is and is not already tracked

`B-compile-material-not-tick-gated` (OPEN, High, encounters 3) covers the **tick-safety** half — the
render flush and live render-state recreation from the handler body, with the verb absent from
`GTickUnsafeMethodNames`. Confirmed still absent: `grep compile_material Dispatch/SafePoint.cpp`
returns one comment hit at `:143` and no table entry. That ticket does **not** cover the unbounded
block, the missing job handle, the mislabelled retryable error, or the concurrency refusal — this
ticket is those four.

`B-compile-material-false-shader-success` (DONE) is the ticket whose fix *introduced*
`WaitAndCollect` / `FinishAllCompilation`. The honesty it bought is worth keeping; the synchronous
shape it bought is what this ticket asks to change.

**Fix:** give the verb a real job handle so a long compile returns a ticket and progresses through
`system.job_status` instead of holding the game thread — the transport already carries the
ticket/SSE machinery, only the handler is synchronous. Independently and much cheaper: teach
`_probe_state` to read `gameThreadStalled` and surface `EDITOR_GAME_THREAD_STALLED` with its
`inFlightMethod` instead of collapsing to a retryable `EDITOR_NOT_READY`; that one change turns an
unexplained four-minute outage into a message naming the call responsible.

## Related

- `B-compile-material-not-tick-gated` (OPEN, High) — the tick-safety half.
- `B-compile-material-false-shader-success` (DONE) — origin of the synchronous wait.
- `B-compile-material-landscape-consumers-stale`, `B-landscape-set-material-stale-mics`,
  `B-material-authoring-save-no-disk-write` — same verb, other axes.

## History
- `#1-unbounded-block-and-a-retryable-lie` `OPEN` reporter — `material.authoring.compile_material` blocks the game thread for the full duration of a cold shader compile: `MaterialAuthoringHandler.cpp:3476` calls `MaterialCompileErrorCollector::WaitAndCollect`, which runs `CacheShaders(EMaterialShaderPrecompileMode::Synchronous)` (`MaterialCompileErrorCollector.h:46`) then `GShaderCompilingManager->FinishAllCompilation()` (`:51`), unbounded and uncancellable, after `:3468` has already flushed rendering commands twice per update context and rebuilt every consuming landscape's MICs. No job handle exists (`grep StartJob` over `Handlers/Material/` = 0 hits; straight-line to `SendSuccess` at `:3531`), and `wait:false` does not help because `bWaitAccepted` (`McpRequestCore.cpp:810-816` → `SocketHttpServer.cpp:997-1002`) only selects ticket-vs-SSE for the HTTP response and leaves the handler synchronous. Past `GameThreadStallReportSeconds` (default 90 s, `PinWrightSettings.cpp:43`) the probe sets `bGameThreadStalled` (`SocketHttpServer.cpp:867-874`) → `editorReady:false` (`McpRequestCore.cpp:380-381`), and `:411` emits the dedicated `EDITOR_GAME_THREAD_STALLED` carrying `inFlightMethod` — which the proxy then throws away: `_probe_state` (`mcp_proxy.py:1596-1601`) tests only for the presence of `editorReady` and never reads `gameThreadStalled`, collapsing to `"not_ready"` → `EDITOR_NOT_READY` (`:3067`, `:3089`) marked `retryable: True` (`:1699`). Every other client is therefore told the editor is still starting up and worth retrying while it is executing their own previous call, which invites the poll loop that keeps the watchdog hot. Concurrency is refused rather than queued (`mcp_proxy.py:3057-3076` probes before forwarding; the in-editor gate at `McpRequestCore.cpp:612` also refuses, and notably does not include `bGameThreadStalled` in its condition). `B-compile-material-not-tick-gated` (OPEN, High) covers only the tick-safety half — the verb is still absent from `GTickUnsafeMethodNames` (`Dispatch/SafePoint.cpp:143` is a comment, not an entry) — and does not cover the block, the missing job handle, the mislabelled error or the refusal. Fix: give the verb a job handle so the transport's existing ticket/SSE path can carry it, and separately teach `_probe_state` to surface `EDITOR_GAME_THREAD_STALLED` with `inFlightMethod` — the cheap half that turns an unexplained four-minute outage into a message naming the responsible call.
- `#2-additional-review-a` `OPEN` reporter — Additional evidence: **Adversarial review A — the root stall survives, but the ticket is overbroad.** Actuality: **CONFIRMED CURRENT**. Framing: `material.authoring.compile_material` still runs straight through `MaterialCompileErrorCollector::WaitAndCollect` (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\Material\MaterialAuthoringHandler.cpp:2693`), whose `CacheShaders(Synchronous)` and `FinishAllCompilation()` are explicit (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\Material\MaterialCompileErrorCollector.h:46`, `:51`); UE 5.8's `FinishAllCompilation` asserts game-thread execution and blocks on all shader maps (`C:\UE_5.8\Engine\Source\Runtime\Engine\Private\ShaderCompiler\ShaderCompiler.cpp:2839`, `:2846`). `PostEditChange()` also reaches UE's `FlushRenderingCommands()` (`C:\UE_5.8\Engine\Source\Runtime\Engine\Private\Materials\Material.cpp:5355`), so the synchronous hazard is current, while the old `ApplyMasterMaterialEdit`/landscape-consumer path in #1 is not present in this handler. The C++ transport does produce `EDITOR_GAME_THREAD_STALLED` with `inFlightMethod` (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Transport\McpRequestCore.cpp:411`, `:417`), but only the bundled proxy collapses any response with `editorReady` present-but-false to `not_ready` (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Content\Python\mcp_proxy.py:1106`) and then maps it to retryable `EDITOR_NOT_READY` (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Content\Python\mcp_proxy.py:2260`, `:1209`); direct `tools/call` intentionally does not gate `bGameThreadStalled` (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Transport\McpRequestCore.cpp:612`). Thus High is plausible for the normal-path game-thread block plus a wrong retry signal, but the title should scope the mislabel to the proxy and mark “four minutes” historical. Proposed fix: **INCOMPLETE**, because `FHandlerContext::StartJob` sends a ticket and then invokes `BindNativeDelegate` synchronously (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\HandlerContext.cpp:627`); wrapping `WaitAndCollect` would preserve the block. Use UE's `BeginCacheShaders(..., Background, ...CompletionCallback)`/`IsCachingShaders` API (`C:\UE_5.8\Engine\Source\Runtime\Engine\Public\MaterialShared.h:2285`, `:2298`) and finish/save/report on completion, serialize same-material jobs, and add proxy stall-state tests plus tick-safety coverage. Evidence: `wait:false` only suppresses the SSE stream gate (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Transport\McpRequestCore.cpp:812`, `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Transport\SocketHttpServer.cpp:965`) while dispatch still hands the handler to the game thread (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Transport\SocketHttpServer.cpp:947`). Runtime: **NOT VERIFIED** (no Unreal run/build in this review). Recommendation: **REFRAME**; split the proxy error-code correction from the material async-compile acceptance and verify cold duration, queued-call behavior, error payload, save timing, cancellation, and direct/proxy responses.
- `#3-additional-review-b` `OPEN` reporter — Additional evidence: **Adversarial review B — A is directionally right, but the retryability claim needs narrower framing.** Actuality: **CONFIRMED CURRENT** for the synchronous blocker: `MaterialAuthoringHandler.cpp:2693` calls `MaterialCompileErrorCollector::WaitAndCollect`, which invokes `CacheShaders(Synchronous)` and `GShaderCompilingManager->FinishAllCompilation()` (`MaterialCompileErrorCollector.h:46`, `:51`); UE 5.8's `FinishAllCompilation` is game-thread-only and blocking. The historical `ApplyMasterMaterialEdit`/landscape-consumer details in #1 are stale, and “four minutes” remains **NOT VERIFIED** in this review. Framing: **PARTIAL / OVERBROAD**. C++ already emits `EDITOR_GAME_THREAD_STALLED` with `inFlightMethod` (`McpRequestCore.cpp:411`, `:417`), and current C++ tests deliberately assert that this stall is still retryable and that direct `tools/call` is not gated (`Private/Tests/Transport/TestGameThreadStallProbe.cpp:264`, `:469`); retryability is therefore not intrinsically the bug. The bundled Python proxy is the mislabel surface: `_probe_state` collapses `editorReady:false` to `not_ready` (`Content/Python/mcp_proxy.py:1106`) and maps it to retryable `EDITOR_NOT_READY` (`:1209`), while direct callers receive the C++ stall diagnostic. The issue should say proxy-facing mislabel and distinguish the direct dispatch behavior. Proposed fix: **INCOMPLETE / SYSTEMIC WORK REQUIRED**. `StartJob` alone cannot help if `BindNativeDelegate` still invokes the synchronous handler; a real fix needs resource-level `BeginCacheShaders(...Background...)` completion, final error collection/save/result publication, same-material serialization (UE's begin path can finish an earlier pending cache), explicit cancellation/timeout semantics, and separate tick-safe dispatch coverage. Proxy normalization must update all state consumers (`_probe`, unavailable-result, process guard, restart, both ready waits, handle mapping) and add stall-state tests, not only `_probe_state`. Evidence: `C:\UE_5.8\Engine\Source\Runtime\Engine\Public\MaterialShared.h:2283-2303`, `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Content\Python\mcp_proxy.py:1055-1243`, `:1873-1988`, `:2198-2283`, and `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Transport\McpRequestCore.cpp:612`. Runtime: **NOT VERIFIED** (no Unreal run/build). Recommendation: **REFRAME and retain High**; split proxy error-code correction from asynchronous material-compile acceptance, then verify cold compile duration, queued/direct/proxy behavior, final errors, save timing, cancellation, and tick safety.
- `#4-bounded-honest-wait-landed-proxy-half-not` `IN-REVIEW` fixer — **Fixed the two halves this verb owns; the proxy half is explicitly NOT fixed and stays open elsewhere.** *Liveness:* `MaterialCompileErrorCollector::WaitAndCollect` no longer calls `GShaderCompilingManager->FinishAllCompilation()`, which has no ceiling, no cancel, and blocks on EVERY pending shader map in the editor plus the whole of `FTextureCompilingManager` (`ShaderCompiler.cpp` `FShaderCompilingManager::FinishAllCompilation`, first statement) — other callers' work, which contributed nothing to this verb's answer because the errors are read off THIS material's `FMaterialResource`. It is replaced by a bounded, PUMPING drain scoped to this material: poll `FMaterial::IsCompilationFinished()` against a 90 s ceiling, driving `FAssetCompilingManager::ProcessAsyncTasks(true)` between polls. The pump and its measured evidence were EXTRACTED from `B-niagara-compile-wait-does-not-wait`'s fix rather than re-derived: `AdvanceAsyncCompilationOnGameThread` moved out of `Handlers/Niagara/NiagaraCompileWait.cpp`'s anonymous namespace into the new `Utils/AssetCompilePump.h` (`PinWright::AssetCompile::AdvanceOnGameThread`), and both waits now call the one implementation. `CacheShaders(Synchronous)` is unchanged: its `FinishCompilation` is scoped to this shader map (`MaterialShader.cpp`, `FMaterialShaderMap::Compile` — High priority submit, then block on CompilingId), it is the verb's advertised contract, and cutting it would regress `B-compile-material-false-shader-success`. **So the verb still blocks for its own compile.** What changed is that it can no longer be held hostage by unrelated queued work, and every wait now has a ceiling. *Honesty:* `compileSucceeded` was `CompileErrors.Num() == 0`, and an empty error list is ALSO what a compile that never ran produces — the verb was reporting the request. It now requires `DidCompileLand(Outcome)`: not timed out, nothing outstanding, and either a complete game-thread shader map or at least one error. `FCompileWaitOutcome` / `DidCompileLand` / `DescribeCompileOutcome` follow the `PinWrightNiagara::MayPersistAfterCompileWait` / `DidCompileLand` house pattern. The response gains `compileStatus` (`completed` | `failed` | `timedOut` | `outstanding` | `notCompiled`) as the single branchable truth, `compileWaitedMs`, and a `warnings[]` entry when nothing landed. Load-bearing measurement detail: a non-null shader map is NOT evidence of a compile — with `IsShaderCompilationSkipped()` the engine forces `PrecompileMode` to `None` and then still calls `SetGameThreadShaderMap` with a zero-shader map (`MaterialShared.cpp`, "We didn't compile any shaders but still assign the result"), so `IsGameThreadShaderMapComplete()` is the predicate. `compiled` is deliberately left a fixed `true`: it is documented as non-verdict legacy in the registration string and every doc page, and changing it is a wire break outside this ticket. *Test:* `PinWright.material.authoring.compile_material.DoesNotClaimSuccessWhenNoCompileLanded` (`Private/Tests/Material/TestCompileMaterialOutcomeHonesty.cpp`) sets the engine's own `FShaderCompilingManager::SkipShaderCompilation(true)`, runs the production handler on a valid material, and asserts the reported outcome against the material's real state — nothing is stubbed and the handler body runs whole. It FAILS before this change (the old handler returns `compileSucceeded: true` beside an empty `compileErrors` for a material the engine never compiled, and `compileStatus` does not exist). It self-reports vacuity through `PinWrightTestSkip` if a host produces a complete shader map anyway, and the success invariant is asserted in both directions. *Files:* `Handlers/Material/MaterialCompileErrorCollector.h`, `Handlers/Material/MaterialAuthoringHandler.cpp` (compile_material only), `Handlers/Niagara/NiagaraCompileWait.cpp` (delegates to the shared pump), `Utils/AssetCompilePump.h` (new), `Docs/wiki-src/material.authoring.md`, plus the new test. **NOT FIXED, and deliberately so.** (1) The proxy mislabel — `_probe_state` collapsing `editorReady:false` to `not_ready` → retryable `EDITOR_NOT_READY` instead of surfacing `EDITOR_GAME_THREAD_STALLED` with `inFlightMethod`. Reviews #2 and #3 are right that it is a separate axis with a systemic blast radius (a new state string has ~10 consumers: `_probe`, `_editor_unavailable_result`, the process guard, restart, both ready waits, handle mapping) and the C++ tests at `Tests/Transport/TestGameThreadStallProbe.cpp` deliberately assert the transport's own retryability. It needs its own ticket and its own Python tests. (2) A real job handle / non-blocking verb, which `StartJob` cannot deliver while `BindNativeDelegate` runs synchronously (`HandlerContext.cpp`) and which needs `BeginCacheShaders(..., Background, completion)` plus same-material serialization. **Reconciliation with `B-compile-material-not-tick-gated`:** genuinely separate causes, and no shared site — that agent added `TEXT("material.authoring.compile_material")` to `GTickUnsafeMethodNames` in `Dispatch/SafePoint.cpp` (arrival POSITION), this ticket changed the wait and the report in `Handlers/Material/` (duration and honesty). Neither fixes the other: gating does not shorten the block, and bounding the wait does not make a mid-tick arrival safe. One stale cross-reference to hand back: that entry's comment cites "`CacheShaders(Synchronous)` plus `GShaderCompilingManager->FinishAllCompilation()`" as the drain it is reasoning about; the `FinishAllCompilation()` half no longer exists. Its conclusion is unaffected (the gate is about position, not contents) but the citation wants a one-line refresh, left to that ticket's owner rather than edited under them.
