---
id: F-sequencer-bake-controlrig
title: "Sequencer bake: anim sequence to Control Rig track and back, space-switch bake"
status: OPEN
severity: High
category: feature
tags: [sequencer, controlrig, bake, animation, parity-ue58]
blockedBy: [F-sequencer-controlrig-track]
---

# Sequencer bake: anim sequence to Control Rig track and back, space-switch bake

No bake operations exist in PinWright's sequencer handlers (grep `bake|Bake` in `Source\PinWright\Private\Handlers\Sequencer\` = zero matches). An agent can neither convert an existing animation into an editable Control Rig track nor export a keyed CR performance back to an AnimSequence asset, which makes the CR cinematics loop a dead end even once F-sequencer-controlrig-track lands.

UE 5.8 parity evidence (`C:\UE_5.8\Engine\Plugins\Experimental\Toolsets\AnimationAssistantToolset\Content\Python\animation_toolset\toolsets\controlrig_sequencer.py` and `sequencer.py`): `bake_to_control_rig`, `export_anim_sequence`, and `bake_space` (space switching with baked transition), over `ControlRigSequencerLibrary` / `SequencerTools`.

Proposed scope:
- `sequencer.bake_to_controlrig(sequence, binding, rigClass, options)` - convert existing anim section keys into a CR track.
- `sequencer.export_anim_sequence(sequence, binding, outAssetPath, range)` - bake the evaluated performance to a new/existing AnimSequence.
- `sequencer.bake_control_space(sequence, binding, control, newSpace, range)` - space switch with baked keys.

Depends on: F-sequencer-controlrig-track.

Acceptance: round trip an AnimSequence -> CR track -> edited key -> exported AnimSequence; exported asset plays with the edit; space bake produces identical world-space pose across the switch frame.

## History
- `#1-no-bake-ops` `OPEN` reporter — No bake in sequencer handlers (grep verified). Epic 5.8 ships bake_to_control_rig / export_anim_sequence / bake_space; both directions plus space-switch bake needed for a usable CR cinematics loop.
- `#2-defer-blocked-cr-track` `OPEN` developer — Deferred, blockedBy F-sequencer-controlrig-track (OPEN, unbuilt). Verified in current source: zero `bake|controlrig` matches in Handlers/Sequencer/ (only SequenceHandler.cpp + SequencerHandler.cpp), and no ControlRigSequencerEditorLibrary / add_controlrig_track anywhere in non-test Source — the parent's typed CR-track surface is genuinely absent from HEAD (a real unbuilt gate, not the code-present anti-pattern). bake_to_controlrig + bake_control_space and the AnimSequence->CR-track->edited-key->exported-AnimSequence acceptance need the parent's CR-track/key/read RPCs first; export_anim_sequence is CR-independent and should be split into its own ticket when un-deferred; proposed bake_control_space signature needs reshaping (FRigSpacePickerBakeSettings + live UControlRig*) against the parent's control model. Red test PinWright.sequencer.bake.ControlRigVerbsRegistered confirms all three verbs unregistered. No code.
- `#3-additional-bake-api-research` `OPEN` reporter — Additional evidence: **Adversarial review A — current gap is real, but the ticket must be split and reframed.** Actuality: CONFIRMED CURRENT. Framing: no sequencer.bake_to_controlrig, sequencer.export_anim_sequence, sequencer.bake_control_space, or direct AnimSequence-to-section RPC is registered; the parent CR-track ticket is now IN-REVIEW with add/list/key/read implemented, so #2's unbuilt-gate premise is stale. UE 5.8 Python names are wrappers; the C++ APIs are UControlRigSequencerEditorLibrary::BakeToControlRig, LoadAnimSequenceIntoControlRigSectionWithRange, BakeControlRigSpace, and USequencerToolsFunctionLibrary::ExportAnimSequence. Export consumes an existing UAnimSequence*, so the handler must own asset create/overwrite/save and set AnimSeqExportOption range. BakeToControlRig can evaluate headlessly through ULevelSequencePlayer, but replaces CR tracks; space bake requires an open Sequencer and live rig, so the proposed combined sequencer.bake_control_space(sequence,binding,control,newSpace,range) contract is underspecified. High remains defensible as a hard CR-loop blocker, not Critical; advanced space bake should be separate/editor-gated. Proposed fix: INCOMPLETE, engine-backed APIs are systemic with typed asset/rig resolution and explicit open-Sequencer gating; direct float-channel copying for evaluated pose or space compensation would be a band-aid. Evidence: <X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\Sequencer\ControlRigSequencerHandler.cpp:440>, <X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\Sequencer\SequencerHandler.cpp:665>, <C:\UE_5.8\Engine\Plugins\Animation\ControlRig\Source\ControlRigEditor\Public\ControlRigSequencerEditorLibrary.h:259>, <C:\UE_5.8\Engine\Plugins\Animation\ControlRig\Source\ControlRigEditor\Public\ControlRigSequencerEditorLibrary.h:295>, <C:\UE_5.8\Engine\Plugins\Animation\ControlRig\Source\ControlRigEditor\Public\ControlRigSequencerEditorLibrary.h:1241>, <C:\UE_5.8\Engine\Plugins\MovieScene\SequencerScripting\Source\SequencerScriptingEditor\Public\SequencerTools.h:190>, <C:\UE_5.8\Engine\Plugins\Animation\ControlRig\Source\ControlRigEditor\Private\ControlRigSequencerEditorLibrary.cpp:1395>, <C:\UE_5.8\Engine\Plugins\Animation\ControlRig\Source\ControlRigEditor\Private\ControlRigSequencerEditorLibrary.cpp:3832>, <X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Tests\Sequencer\TestSequencerControlRigTrack.cpp:72>. Runtime: NOT VERIFIED. Recommendation: REFRAME; recheck/remove blockedBy only after the parent tester, split direct AnimSequence load/evaluated bake/export from open-Sequencer space switching, then add headless round-trip and world-space switch tests.
- `#4-additional-adversarial-bake-split` `OPEN` reporter — Additional evidence: **Adversarial review B — A's current-gap finding stands, but the split must be made contractual.** Actuality: CONFIRMED CURRENT. Framing: the parent CR-track work is now IN-REVIEW, while current Sequencer handlers expose only add/list/key/read (`ControlRigSequencerHandler.cpp:440`, `:618`, `:680`, `:840`); no bake/export/space verb or bake test exists. UE's `BakeToControlRig` bakes the binding's evaluated animation, creates a transient AnimSequence, and removes existing CR tracks (`ControlRigSequencerEditorLibrary.cpp:1395`, `:1424`, `:1445`); direct AnimSequence-to-CR loading is a separate API and needs the legacy 5.3-5.5 call versus the range-aware 5.6+ call. `ExportAnimSequence` accepts an existing UAnimSequence, not an asset path, so create/overwrite/save/link/range and destructive replacement require explicit policy. Space bake takes typed `TargetSpace` plus `FRigSpacePickerBakeSettings` and requires an open Sequencer (`ControlRigSequencerEditorLibrary.h:1241`, `.cpp:3832`), so `newSpace` alone is underspecified. High remains defensible for the end-to-end CR-loop blocker, though export-only may be Medium after splitting. Proposed fix: INCOMPLETE, engine-backed paths are systemic; float-channel copying would be a band-aid. Evidence: <X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\Sequencer\ControlRigSequencerHandler.cpp:440>, <X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\Sequencer\SequencerHandler.cpp:665>, <C:\UE_5.3\Engine\Plugins\Animation\ControlRig\Source\ControlRigEditor\Public\ControlRigSequencerEditorLibrary.h:140>, <C:\UE_5.8\Engine\Plugins\Animation\ControlRig\Source\ControlRigEditor\Public\ControlRigSequencerEditorLibrary.h:259>, <C:\UE_5.8\Engine\Plugins\MovieScene\SequencerScripting\Source\SequencerScriptingEditor\Public\SequencerTools.h:190>, <C:\UE_5.8\Engine\Plugins\MovieScene\SequencerScripting\Source\SequencerScriptingEditor\Private\SequencerTools.cpp:391>, <X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Tests\Sequencer\TestSequencerControlRigTrack.cpp:72>. Runtime: NOT VERIFIED. Recommendation: REFRAME; split direct AnimSequence load/evaluated bake/export from open-Sequencer space conversion, add version gates and non-destructive replacement guards, then verify asset persistence, round-trip edits, and world-space invariants.
- `#5-two-of-three-verbs-landed` `OPEN` developer — Landed 2 of the 3 proposed verbs in a new `Handlers/Sequencer/SequencerBakeHandler.cpp`; the space-switch half is NOT built and this ticket stays OPEN for it. **Blocker check first:** F-sequencer-controlrig-track's IN-REVIEW claim is real in THIS checkout (`unreal-fpv-new`, not the `unreal-fpv-dev` path reviews #3/#4 cite) — `ControlRigSequencerHandler.cpp` is committed (41 KB, 4 verbs registered at :440/:618/:680/:840, FK bind-then-Initialize, version-gated 5.3-5.8 channel mapping) with a 30 KB round-trip test `PinWright.Sequencer.ControlRigTrack.AddKeyReadback`; only wiki-src docs for those 4 verbs are missing (no infra guard requires them). Foundation judged solid; built on it. **Landed:** `sequencer.bake_to_controlrig` over `UControlRigSequencerEditorLibrary::BakeToControlRig` and `sequencer.export_anim_sequence` over `USequencerToolsFunctionLibrary::ExportAnimSequence` — both engine-backed (transient `ULevelSequencePlayer` + `MovieSceneToolHelpers::ExportToAnimSequence`, no hand-rolled key transfer) and both headless-viable. Per response-honesty: bake counts the keys actually on the new CR section's float channels (`keysWritten`, `channelsWithKeys`/`channelCount`, `keyRange` in display frames, `replacedControlRigTracks` measured BEFORE the call because the engine destroys existing CR tracks) and export walks the written asset's data model (`boneTrackCount`, `boneKeysWritten`, `numberOfFrames`, `frameRate`, `playLengthSeconds`); either verb refuses new `NO_KEYS_WRITTEN` rather than reporting a success full of zeroes when the engine returns true having written nothing. Export owns asset create/overwrite/save (`ASSET_ALREADY_EXISTS` gate, `INVALID_ASSET_TYPE` on a non-AnimSequence, `AddAssetSaveReport` persistence verdict) and passes `bCreateLink=false` so it never mutates the sequence; `startFrame`/`endFrame` map to `UAnimSeqExportOption`'s custom time range and are refused as UNSUPPORTED_ENGINE_VERSION below 5.5 rather than silently widened. New codes `BAKE_FAILED` + `NO_KEYS_WRITTEN` in `Handlers/ErrorCodes.h`. Tests: `PinWright.Sequencer.ControlRigBake.AnimSequenceRoundTrip` (bake → list/key/read the baked control → export → re-export refused) and `.RefusesNonSkeletalBinding` (host-independent, asserts no track and no asset is created by a refusal); `check_test_ids.py` CLEAN. Docs: new `## Baking to and from a Control Rig track` section plus both `### ` method sections in `Docs/wiki-src/sequencer.md`. **NOT built, and why:** `sequencer.bake_control_space`. `UControlRigSequencerEditorLibrary::BakeControlRigSpace` (`ControlRigSequencerEditorLibrary.cpp:3831`) early-outs on `WeakSequencer.IsValid()` and returns a bare false with no diagnostic when no Level Sequence editor is open, so it cannot run headless at all; worse, it calls `FindSpaceChannelAndSectionForControl(..., bCreateIfNeeded=false)` and returns TRUE having baked nothing for a control with no space channel — the exact silent-zero shape this ticket exists to prevent. A space channel is created only by `SetControlRigSpace`, which PinWright has no verb for. Remaining work, precisely: (1) add `sequencer.set_control_space` (also open-Sequencer gated) so a space channel can exist; (2) `bake_control_space` with an explicit open-Sequencer precondition error instead of the engine's silent no-op, a `TargetSpace` shaped as an `FRigElementKey` (name AND element type, world space = invalid key) rather than #1's single `newSpace` string, and the `FBakingAnimationKeySettings` range/KeysOnly-vs-AllFrames/FrameIncrement/reduce/tolerance fields; (3) measured before/after space-channel and transform-channel key counts; (4) the world-space-pose-across-the-switch-frame acceptance, which needs a live editor. NOT COMPILED and NOT RUN — no build or test runner was available to this agent; the orchestrator builds and runs the suite.
