---
id: B-foliage-paint-does-no-ground-projection
title: "foliage.paint runs no trace and no projection — it writes each instance at the literal XYZ, never reaching FPotentialInstance::PlaceInstance, so a caller who reads the verb's name gets foliage buried in or floating over the ground and a green instancesPlaced count (title narrowed at #6: the rotation and scale clauses moved to their own tickets)"
status: DONE
severity: High
category: bug
tags: [foliage, paint, add_instances, ground-projection, placement, silent-wrong-output, no-readback, world-origin, silent-drop, misleading-verb-name]
lastSeen: 2026-08-30T16:00:00+05:00
---

# `paint` is `add_instances` with a weaker argument shape and a name that promises otherwise

`foliage.paint` is registered as *"Paint foliage instances at specified locations"*
(`Handlers/Environment/FoliageHandler.cpp:75`). Its entire placement loop is
`Handlers/Environment/FoliageHandler.cpp:221-238`:

```cpp
FFoliageInstance Instance;
Instance.Location = Location;
Instance.Rotation = FRotator::ZeroRotator;
Instance.DrawScale3D = FVector3f(1.0f);
Instance.ZOffset = 0.0f;
```

(`:222-226`, then `Info->AddInstance(FoliageType, Instance, nullptr)` at `:229` / `:233`.)

No line trace. No surface projection. No normal alignment. No Z offset. No random yaw. The
instance goes exactly where the caller's number said, pointing straight up, at scale 1.

## What "paint" means in the engine, and where this verb stops short

The engine's own single-instance placement routine is
`FPotentialInstance::PlaceInstance`
(`C:/UE_5.8/Engine/Source/Runtime/Foliage/Private/InstancedFoliage.cpp:5506`). Its inputs are a
`HitLocation`, a `HitNormal` and a `HitComponent` — i.e. the result of a trace
(`FPotentialInstance`'s constructor, `:5497`). From those it does everything the word *paint*
implies:

- `Inst.Location = HitLocation;` (`:5523`) — the **hit**, not the requested point
- random pitch and yaw from the type's `RandomPitchAngle` / `RandomYaw` (`:5527-5537`)
- `if (Settings->AlignToNormal) { Inst.AlignToNormal(HitNormal, Settings->AlignMaxAngle); }`
  (`:5546-5549`)
- `ZOffset` applied in the instance's local space, i.e. along the aligned normal (`:5551-5554`)
- a collision check against the world (`AInstancedFoliageActor::CheckCollisionWithWorld`, `:5432`)

`foliage.paint` reaches none of it. It hand-builds an `FFoliageInstance` and calls
`FFoliageInfo::AddInstance` directly, which is the storage call, not the placement call.

**This is also why the foliage type's own settings are inert for anything PinWright places.**
`foliage.add_type` accepts `alignToNormal` (`FoliageHandler.cpp:496`, parsed `:544-545`) and writes
it to `FoliageType->AlignToNormal` (`:618`) — and that property is read in exactly one place,
`PlaceInstance` at `:5546`, which needs a `HitNormal` no PinWright verb produces. A reader skimming
the handler sees an `alignToNormal` parameter in the same file and concludes the gap is covered.
**It is not**: that parameter belongs to a different verb, sets a type-level property, and governs
the interactive Foliage editor's brush, not this verb's writes. Calling this out explicitly because
it is the most likely way this ticket gets wrongly closed.

## Two silent-input defects in the same handler

**Non-object entries in `locations[]` vanish with no record.** The parse loop
(`FoliageHandler.cpp:126-137`) adds a point only when `Val->Type == EJson::Object`; anything else —
a number, a string, an array-form `[x,y,z]` that `foliage.add_instances` accepts — falls through
with no `skipped[]`, no `skippedCount`, and no reason. Its sibling `foliage.add_instances` was fixed
to report exactly this (`NoteSkipped`, `:695`; registration text at `:640` promises it); `paint`
never was.

**An entry with no `x`/`y`/`z` is placed at the world origin.** `double X = 0, Y = 0, Z = 0;`
followed by three `TryGetNumberField` calls that leave the zero in place on a miss
(`:130-134`, and the same shape in the single-`position` branch at `:145-148`). `{}` becomes
`FVector(0,0,0)`. This is the exact behaviour `Docs/wiki-src/level-review.evidence-and-provenance.md:84`
attributes to `actor.spawn_batch` and `foliage.add_instances` — both of which now report `skipped[]`
instead. The doc names the two verbs that were fixed and misses the one that still does it. Filed
separately as `B-evidence-provenance-foliage-skipped-claim-stale`; recorded here because the fix for
`paint` is what makes that doc line correctable rather than merely deletable.

## What the response does and does not say

`:244-250`: `success`, `foliageTypePath`, `instancesPlaced`, `foliageActorPath`, `foliageActorName`,
and `existsAfter: true` — **hardcoded**, not read back. Nothing about where the instances ended up
relative to any surface, and no field a caller could check to discover that no projection happened.
This is the recurring shape: the call succeeds, `instancesPlaced` is correct, and the output is
wrong because the deciding fact — did anything land on the ground — was never reported.

## Side effect worth knowing about

Given a `foliageTypePath` that loads as a `UStaticMesh` rather than a `UFoliageType`, the verb
**creates and saves a new asset**: `/Game/Foliage/Auto_<MeshBaseName>`, a
`UFoliageType_InstancedStaticMesh` with a hardcoded `Density = 100.0f` (`:170-200`, density at
`:193`). The response's `foliageTypePath` then echoes the auto-created path, so a caller who passed
a mesh gets a content-folder asset they did not ask for and did not name. Not this ticket's defect,
but a fixer changing this code path should not be surprised by it.

## Fix

Two coherent options; the second is preferred.

1. **Make `paint` paint.** Add an optional `projectToGround` (default **true**, since the name
   already promises it) that traces down from each supplied XY, places at the hit, and applies the
   foliage type's `AlignToNormal` / `RandomYaw` / `ZOffset` — ideally by routing through
   `FPotentialInstance::PlaceInstance` rather than re-implementing it, so the type's settings and
   the engine's collision check apply for free. Report per-instance `projected: true/false` and the
   hit component, so an unprojected instance is visible in the response. Reuse the `surface` filter
   vocabulary the `spatial` verbs already speak (see `E-ground-preset-excludes-only-foliage-actors`
   for why the preset matters here) rather than inventing a second one.
2. **Or retire the name.** If projection is not going to be implemented, `paint` is a strictly
   weaker duplicate of `foliage.add_instances` — same destination, fewer accepted forms, no
   `skipped[]`, no per-instance rotation or scale — and the honest move is to deprecate it, point
   its doc at `add_instances`, and stop advertising a behaviour the code does not have. A verb that
   cannot do what its name says is worse than a missing verb, because a missing verb sends the
   caller looking.

Either way, fix the two input defects: report `skipped[]` on a non-object entry, and refuse an entry
with no resolvable location instead of silently placing it at the world origin.

## Adjacent, not folded in

`FoliageHandler.cpp` contains **zero** `FScopedTransaction` occurrences — every mutating foliage
verb (`paint`, `remove`, `add_instances`, `create_procedural`, `add_type`) calls `IFA->Modify()`
(`:240`, `:343`, `:349`, `:898`) with no transaction, against `agent-conventions.md`'s *"Wrap EVERY
mutation in `FScopedTransaction`"*. `B-no-undo-redo` (DONE) covered the BPIR/widget verbs only and
does not mention foliage. Not filed here because it is a namespace-wide gap deserving its own
ticket, not a `paint` defect.

## Same shape as

`B-ground-probe-hits-hull-not-render`, `B-niagara-validate-green-while-component-inactive`,
`B-mrq-render-result-omits-bitrate-and-size` — the call succeeds, every number it reports is
correct, and the output is wrong because the deciding number was never reported.

`F-scatter-layout-verb` is the same underlying capability seen from the other side: placement that
respects the ground. That is a missing *layout* verb and this is a defective *placement* verb, so
they are deliberately split — a fixer could land either without the other — but whoever takes one
should read the other, because a scatter layout that hands its transforms to a `paint` that does not
project produces exactly the floating vegetation both tickets exist to prevent.

## Not RPC-verified

Source-read only; the editor was not running for this pass. No `foliage.paint` call was made and no
placed instance was inspected. An editor test would settle one thing worth knowing before choosing
between the two fixes above: whether `FFoliageInfo::AddInstance` applies *any* of the foliage type's
placement settings on its own path (density culling, `ZOffset`, random yaw). Reading
`PlaceInstance` says it does not — every one of those is applied before `AddInstance` is reached —
but that is an inference from the engine's call order, not an observation.

severity rationale: impact=High — silent wrong output on a normal path: the verb's name, its registered summary and the whole vocabulary of foliage painting promise surface projection, the caller builds a scatter on that promise, and the response hardcodes `existsAfter: true` and publishes nothing that could reveal the instances are unprojected; compounded by two literal silent-input defects in the same handler (non-object entries dropped without `skipped[]`, location-less entries placed at the world origin) × reach=normal — `foliage.paint` is one of six verbs in the namespace and the one a caller reaches for first, which is not a "rare edge path" in the rubric's sense, so the bump-down is declined; a reviewer who reads reach as observed usage (this namespace has never been used on this project — zero `InstancedFoliageActor` across ~8,800 placed instances in three builds) lands on Medium, and that is the specific reading being rejected, because observed usage is an encounters signal and the rubric excludes it as a severity input -> High

## History
- `#1-paint-performs-no-trace` `OPEN` reporter — Source-read only, editor not running; no `foliage.paint` call was made. The verb's whole placement loop is `FoliageHandler.cpp:221-238`: `Instance.Location = Location` (`:223`), `Rotation = FRotator::ZeroRotator` (`:224`), `DrawScale3D = FVector3f(1.0f)` (`:225`), `ZOffset = 0.0f` (`:226`), then `FFoliageInfo::AddInstance` (`:229`, `:233`). No trace, no projection, no normal alignment, no random yaw. The engine's actual placement routine is `FPotentialInstance::PlaceInstance` (`InstancedFoliage.cpp:5506`), driven entirely by a trace hit — `Inst.Location = HitLocation` (`:5523`), random pitch/yaw (`:5527-5537`), `AlignToNormal(HitNormal, ...)` (`:5546-5549`), local-space `ZOffset` (`:5551-5554`), world collision check (`:5432`) — and `foliage.paint` never reaches it. Trap flagged explicitly in the body because it is how this gets wrongly closed: `alignToNormal` DOES appear in the same file (`:496`, `:544-545`, `:618`) but belongs to `foliage.add_type`, sets the type-level `UFoliageType::AlignToNormal`, and that property is read in exactly one place — `PlaceInstance:5546` — so it is inert for anything this plugin places. Two further silent-input defects found in the same handler: non-object `locations[]` entries are dropped with no `skipped[]` (`:126-137`), and an entry with no `x`/`y`/`z` is placed at the world origin because the `double X = 0, Y = 0, Z = 0` initialisers survive a failed `TryGetNumberField` (`:130-134`, `:145-148`) — which is exactly what `level-review.evidence-and-provenance.md:84` wrongly attributes to `add_instances` and `spawn_batch`. Response hardcodes `existsAfter: true` (`:250`) and carries nothing about the placement. Dedup: searched the board for `foliage.paint`, `foliage.add_instances`, projection, `AlignToNormal`, scatter and every `B-foliage-*` / `E-foliage-*` file. `B-foliage-create-procedural-empty-callback-noop` (IN-REVIEW) is a different verb and a different mechanism, and is confirmed fixed in this tree — the reflection path through `UProceduralFoliageEditorLibrary::ResimulateProceduralFoliageComponents` is present at `FoliageHandler.cpp:1176-1194`. `E-foliage-get-instances-drops-scale` (IN-REVIEW), `E-foliage-nested-input-schemas-undocumented` (IN-REVIEW), `E-foliage-remove-silent-edge-inputs` (IN-REVIEW) and `E-foliage-add-type-auto-save-undocumented` (WONTFIX) are readback, docs, `remove`, and `add_type` respectively. Nothing on the board mentions `foliage.paint` beyond its name. Recorded but deliberately NOT filed here: `FoliageHandler.cpp` contains zero `FScopedTransaction` occurrences across all five mutating verbs, which is a namespace-wide gap `B-no-undo-redo` (DONE, BPIR-scoped) does not cover.
- `#2-projection-routed-through-seatinstance` `IN-REVIEW` fixer — Every mechanism claim in the body verified in source before acting; all held. `foliage.paint`'s loop did write `Location`/`ZeroRotator`/`FVector3f(1)`/`ZOffset 0` straight into `FFoliageInfo::AddInstance`, and the engine side is confirmed too: `AddInstanceImpl` (`InstancedFoliage.cpp:2275`) only appends to `Instances`, stamps a base id and inserts into `InstanceHash` — it applies *no* placement, so the ticket's "Not RPC-verified" inference about `AddInstance` is correct by source read. `PlaceInstance` (`:5506`) is trace-driven exactly as described. Two ticket details are off: `TraceGroundBelow` lives in `SpatialTraceUtils`, not `GroundPlacementUtils`, and `AInstancedFoliageActor::SetFoliageInstanceTransform` — the obvious engine route for the write — is **unusable from a plugin module**: the class is `MinimalAPI` and that member carries no `FOLIAGE_API`, so it would not link. FIX (option 1, routed, not re-implemented): supplying `surface` now seats each instance through `GroundPlacement::SeatInstance(..., bApply:false)` — the canonical solve, taken as a DRY RUN — and the write goes through `FFoliageInfo::PreMoveInstances` / `Instances[i].Location` / `PostMoveInstances`, because `SeatInstance`'s own applying path is `UpdateInstanceTransform`, which would move the render instance and leave the foliage record and its location hash pointing at the old Z. No new ground math: the percentile/embed solve is never re-derived. `EmbedFraction` is forced to 0 so foliage rests on the surface rather than bedding into it. The component is fetched INSIDE the loop, not before it: `FFoliageStaticMesh` creates its HISM lazily in the first `AddInstance` (`PreAddInstances` -> `Initialize` -> `CreateNewComponent`), so `GetComponent()` is null right after `AddFoliageType`; the up-front refusal is `IsA<UFoliageType_InstancedStaticMesh>` instead. An instance whose column finds no accepted ground is REMOVED (`RemoveInstances` on the last index, a plain truncation) rather than left floating, and reported in `skipped[]` with the solve's own reason. DEFAULT IS OPT-IN, deliberately: `projectToGround` is true iff `surface` was supplied. Default-ON would have had to invent a preset, which the ground module explicitly forbids, and would have broken `TestFoliagePlacementBehaviour.cpp` (a sibling agent's in-flight `F-foliage-namespace-has-no-behavioural-tests` file, written 08:23 today, which paints without a surface and states this ticket "carries its own regression test for the projection itself"). `projectToGround:true` with no `surface` is `INVALID_SURFACE_SPEC`, never a guess. What closes the silent-wrong-output defect on BOTH branches is that `projected` is now on every response, the unprojected branch adds a `warnings[]` line naming `surface` as the remedy, and the projecting branch adds `projectedCount`, `surfacePreset` and a `placed[]` row per instance carrying the reached XYZ and the signed `deltaZCm`. Both input defects fixed too: a non-object entry and an entry missing any of x/y/z now land in `skipped[]`/`skippedCount` (32-row cap, `skippedTruncated`) instead of vanishing or being placed at the world origin; a coordinate-less single `position` is a hard `INVALID_ARGUMENT`. `groundProvenance` is NOT published here — `GroundRpcProvenanceObject` is file-local to `GroundPlacementHandler.cpp`, which `E-ground-preset-excludes-only-foliage-actors` is being worked in right now, so lifting it was left alone. Files: `Handlers/Environment/FoliageHandler.cpp`, new `Tests/Environment/TestFoliagePaintGroundProjection.cpp` (3 behavioural tests, each red before the fix: `paint.SeatsInstancesOntoTheNamedSurface` drops an instance 800 cm onto a spawned floor and re-reads Z from BOTH `FFoliageInfo::Instances` and the HISM's own instance transform, so a write that moved one and not the other fails; `paint.UnprojectedBatchDisclosesThatItDidNotProject`; `paint.LocationWithoutCoordinatesIsReportedNotPlacedAtTheOrigin`), and a `### foliage.paint` overlay section in `Docs/wiki-src/foliage.md`. Not compiled or run — the wave owner builds. Still untouched and still open elsewhere: the missing `FScopedTransaction` across every mutating foliage verb, and the `/Game/Foliage/Auto_<Mesh>` side-effect asset.
- `#3-provenance-published-per-instance` `IN-REVIEW` developer — The one thing `#2-projection-routed-through-seatinstance` could not do — publish `groundProvenance` — now landed. Its blocker was real and is gone: `GroundRpcProvenanceObject` was file-local to `GroundPlacementHandler.cpp`, which a sibling agent held for `E-ground-preset-excludes-only-foliage-actors`. **The writer was LIFTED, not copied.** It moved verbatim to `GroundPlacement::MakeProvenanceJson(const FGroundContactReport&)` in `GroundPlacementUtils.h/.cpp` — the module that already owns `FGroundProvenance` and already includes `Dom/JsonObject.h` for `ParseSurfaceJson` — and the handler's own writer is deleted, its single call site now deferring to the shared one with the emitted text unchanged byte for byte. So `placed[]`'s block is not merely the same SHAPE as the ground verbs': it is the same function's output, which is what makes the two incapable of drifting. The other `GroundRpc*` writers (`GroundRpcVectorObject`, `GroundRpcTransformObject`, `GroundRpcContactObject`, `GroundRpcSeatResultObject`) stay file-local — they are response-envelope shapes only `spatial.*` emits; provenance is the one block that crossed a handler boundary, so it is the one that moved. **What `foliage.paint` publishes:** each `placed[]` row now carries `groundProvenance` for that instance's own probe — `traceComplex`, `triangleColumns` / `primitiveColumns`, `renderGeometryColumns`, the simple-shape range, the hull `warning`, and (new in the same pass, `E-ground-preset-excludes-only-foliage-actors` `#3`) `surfaceComponents[]` naming the `{actor, component, componentClass, columns}` of the primitive that answered. Read off `Seat.Seat.Contact`, which on the `bApply:false` path is the pre-move probe, so it describes the solve that actually placed the instance. Gated on `SupportedColumns > 0`, the identical condition `GroundRpcContactObject` gates on — with no supported column the block would describe nothing, and an empty one reads as an answer. Per-instance rather than per-batch on purpose: a scatter can straddle terrain and a neighbouring HISM, and one batch-level block would have had to pick. It rides the existing 32-row `placed[]` cap; no new cap and no new count. **Regression test** `Tests/Environment/TestFoliagePaintGroundProjection.cpp` (appended beside `#2`'s three, same fixtures, same helper namespace): `PinWright.foliage.paint.ProjectionPublishesTheSameGroundProvenanceAsTheSpatialVerbs`. It is an EQUIVALENCE, not a shape check, because a second hand-written copy of the block would pass a shape check and still be free to drift: one floor answers two probes — a painted foliage instance seated on it at the column centre, and an ordinary prop verified by `spatial.verify_grounding` on the same floor 120 cm away, clear of the 100 cm instance — and every field the two publish about that primitive must agree (`actor`, `component`, `componentClass`, `traceComplex`, `surfaceComponentCount`). Plus the discriminating half: the named surface must be the FLOOR actor and a `StaticMeshComponent`, never the foliage's own instanced component, so a verb reporting the primitive it had just written into cannot pass by agreeing with itself. Red before the change: `placed[]` rows carried `{index, x, y, z, deltaZCm}` and no `groundProvenance` at all, so the first assertion fails outright. **Not compiled and not run** — a full automation suite was running against the compiled DLL throughout this pass. Docs: the `### foliage.paint` overlay in `Docs/wiki-src/foliage.md` now lists `groundProvenance` in the `placed[]` row shape with a paragraph on why to read it (an instance seated on a neighbouring HISM instead of on terrain is invisible in `z` and `deltaZCm` alone; `componentClass` is where it shows) and cross-links `spatial.ground-placement`; the registration summary says the same in one clause. **Noted, not fixed:** `foliage.paint`'s `surface` parameter description enumerates the filter fields and does not list `excludeComponentClasses`, which its parser (`GroundPlacement::ParseSurfaceJson`) has accepted since `E-ground-preset-excludes-only-foliage-actors` `#2` — that ticket updated the two enumerating strings in `GroundPlacementHandler.cpp` and could not have known about this third copy, which landed concurrently. A one-line doc gap in a third schema string, left for whoever owns the duplicate-parser cleanup. Still untouched and still open elsewhere, unchanged from `#2`: the missing `FScopedTransaction` across every mutating foliage verb, and the `/Game/Foliage/Auto_<Mesh>` side-effect asset.
- `#4-projection-verified-done-withheld-on-the-title` `IN-REVIEW` tester — **Live verification of `#2` and `#3`: the PROJECTION subject is verified working. DONE is withheld, and the reason is a sentence in this ticket's own title, not the aside discussed at the end of this entry.** What was measured, against a running editor on a 33.9-degree slope (surface normal `(0.006, 0.558, 0.830)`), four instances, `surface` supplied: the response reported `projected: true`, correct per-instance `deltaZCm`, and a `groundProvenance` block per `placed[]` row. That is `#2`'s seat-through-`SeatInstance` and `#3`'s lifted `GroundPlacement::MakeProvenanceJson` both doing exactly what they claim. No part of the projection fix is challenged. **Why not DONE.** The title says the verb "writes each instance at the literal XYZ **with a zero rotator and unit scale**" and lists "**no normal alignment**" among the three defects. At HEAD those two clauses are still true, re-derived this session: the placement loop (now `FoliageHandler.cpp:457-539`) still constructs `Instance.Rotation = FRotator::ZeroRotator` (`:463`) and `Instance.DrawScale3D = FVector3f(1.0f)` (`:464`) as constants, and the projection write is translation-only — `Placed = Seat.ProposedTransform->GetTranslation()` (`:505`) into `Instances[InstanceIndex].Location` (`:512`) inside the `PreMoveInstances`/`PostMoveInstances` bracket (`:511-513`), with no rotation write anywhere. Measured consequence: all four instances read back `pitch 0, yaw 0, roll 0` on a 33.9-degree slope, while the `UFoliageType` the verb auto-created carries `AlignToNormal = True` and `RandomYaw = True` (from the engine constructor, `C:/UE_5.8/Engine/Source/Runtime/Foliage/Private/InstancedFoliage.cpp:580-586`). `grep -n "AlignToNormal\|RandomYaw" FoliageHandler.cpp` returns four hits (`:87`, `:99`, `:129`, `:958`), all WRITES on the `add_type` / `create_procedural` type-authoring paths — the file contains no read. So a DONE here would close a ticket whose title still describes a live defect. **Recorded as a SPLIT, not as a return.** The rotation half is now `B-foliage-paint-ignores-align-to-normal-and-random-yaw` (OPEN, High, filed this session), which carries the full analysis, the engine parity citation (`FPotentialInstance::PlaceInstance` `:5506`, `RandomYaw` `:5530-5532`, `AlignToNormal` `:5546-5548`), and the fix. Status is deliberately left `IN-REVIEW` rather than returned to `OPEN`: a return would put the fix picker onto work that split ticket now tracks in full, which is duplicate effort on one defect and the thing the board's dedup and no-umbrella rules exist to prevent. **The cheapest correct resolution for whoever owns this ticket is to narrow the title to the projection subject** — the alignment and scale clauses now have their own home — at which point everything this ticket claims is verified and it is DONE. That is a scope decision for the owner, not a tester's to make. It is also worth recording that the rotation defect was made VISIBLE by this ticket's own fix: before `#2`, an unoriented plant and an unplaced one were indistinguishable, both reading as "the verb did nothing useful". One defect uncovering the next is the fix working, not the fix failing. This measurement also settles the open question in this ticket's "Not RPC-verified" section — whether `FFoliageInfo::AddInstance` applies any placement settings on its own path. It does not; the inference `#2` confirmed by source read is now confirmed by observation. **On the "Side effect worth knowing about" section (`:83-86`), which asserts the verb "creates and saves a new asset" `/Game/Foliage/Auto_<MeshBaseName>`: "saves" is FALSE at HEAD, and it is NOT the reason DONE is withheld.** Verified: the auto-create path (`FoliageHandler.cpp:383-393`) calls `McpSafeAssetSave(AutoFT)` (`:391`), which is `void McpSafeAssetSave` (`Private/Utils/AssetUtils.cpp:362-377`) — `MarkPackageDirty()` + `FAssetRegistryModule::AssetCreated()` and nothing else. Its header comment (`Private/Utils/AssetUtils.h:102-113`) is explicit: it "deliberately does NOT write the .uasset ... so NOTHING it does makes an edit durable", and it returns `void` precisely because callers were publishing its old constant-`true` return as a `saved` field. The path `/Game/Foliage/Auto_<BaseName>` (`:372`) and the "creates" half are correct. **Concluded: incidental aside, not a verification blocker** — it is a parenthetical in a section the ticket itself labels a side effect, it is not in the title or the subject, it is not something `#2` or `#3` claimed to fix, and it is now owned by `B-add-instances-auto-foliage-type-name-mismatch` (filed this session), which is the right place for it. Left uncorrected here on purpose: history is append-only, that ticket owns the claim, and duplicating the correction into this body would put two authorities on one fact. Had the title clause not been outstanding, this aside alone would not have stopped a DONE. NOT DONE in this entry: no source modified, no status change, no existing prose edited, and only the projected branch was exercised — the unprojected branch writes the same rotation constant at `:463` by source read and was not called.
- `#5-rotation-blocker-gone-in-source-still-unverified-live` `IN-REVIEW` tester — 2026-08-30, **13:32 build** (plugin `d8f1bc32`, waves 2 and 3). **Status deliberately unchanged, and the reason is that the one thing that would settle it could not be run.** `#4` withheld DONE on two title clauses — "zero rotator and unit scale" and "no normal alignment". **The rotation half of that blocker is implemented at HEAD `1a9e5778`**, every citation re-derived here rather than inherited, and every one of `#4`'s is now stale: the placement loop has moved off `:457-539`, `Instance.Rotation = FRotator::ZeroRotator` is **gone** from `:463`, and in its place `FoliageHandler.cpp:843-846` reads `bTypeRandomYaw = FoliageType->RandomYaw != 0`, `TypeRandomPitchAngle`, `bTypeAlignToNormal` and `TypeAlignMaxAngle` off the resolved type; `:908-915` writes `Instance.Rotation` from them (`FOLIAGE_NoRandomYaw` set at `:914` when the type says no); `:975-979` calls `Instances[InstanceIndex].AlignToNormal(GroundNormal, TypeAlignMaxAngle)` against the seat's `Contact.AverageNormal` (`:976`, gated on `SupportedColumns > 0` at `:977`); and the response now carries a `rotation` block (`:1041-1049`: `randomYaw`, `randomPitchAngleDeg`, `alignToNormal`, `alignMaxAngleDeg`, `alignedCount`) plus per-instance `pitch`/`yaw`/`roll` and `alignedToNormal` in the `placed[]` rows (`:871-878`). `#4`'s "the file contains no read" finding is therefore no longer true. The fix is tracked by `B-foliage-paint-ignores-align-to-normal-and-random-yaw` `#2` (that ticket is now IN-REVIEW, not the OPEN `#4` names).

  **Why that is not enough to close, and it is the same reason `#4` gave for not trusting a number.** This is a **source reading, not a measurement**. The defect `#4` measured was exactly a case where the code looked like it placed foliage and the rotators all came back zero, so a rotation fix that has never been observed on a slope is precisely the claim this ticket's own history says not to take on trust. An in-editor re-run was attempted this session and never happened: the shared editor (pid 7856) was killed by an unrelated Critical while the scratch fixture for it was being created — `B-foliage-add-type-name-with-slash-kills-the-editor` — and could not be restarted.

  **Exactly what settles it**, so the next tester does not have to re-derive the shape: paint at least four instances on a slope steeper than the type's `AlignMaxAngle` with `surface` supplied and `projectToGround` on; assert from `foliage.get_instances` (not from the verb's own `rotation` echo, which reports the type's settings rather than what was written) that the read-back `pitch`/`roll` are non-zero and consistent with the measured surface normal, and that `yaw` varies across the four; then repeat with a type carrying `AlignToNormal=false, RandomYaw=false` and assert the rotators are zero, which is the control that separates "alignment ran" from "some rotation happened". `#4`'s slope (33.9°, normal `(0.006, 0.558, 0.830)`) is a known-good fixture. Also still unexercised, as `#4` noted: the **unprojected** branch.

  **On the title, which is the actual blocker — a recommendation, not a change.** Two of the three clauses have moved out: rotation/alignment to `B-foliage-paint-ignores-align-to-normal-and-random-yaw` (IN-REVIEW, fix landed), scale/ZOffset to `B-foliage-paint-hardcodes-scale-and-zoffset` (OPEN, Medium, still live — `FoliageHandler.cpp:919-920` still writes `DrawScale3D = FVector3f(1.0f)` and `ZOffset = 0.0f`). The subject clause, "does no ground projection", was verified false by `#4`. So the title now misdescribes the ticket in both directions at once. `#4` is right that narrowing it to the projection subject is the cheapest correct resolution and right that it is the owner's call, not a tester's — this entry does not make it. **Once narrowed, the remaining bar is the live rotation re-run above**, because the projection subject itself is already verified.

  **One citation correction for whoever narrows it.** The code now concedes the scale/offset hardcoding in a comment at `FoliageHandler.cpp:916-918` and in the verb's own registered description (`:548`, "Instance SCALE and ZOffset are still hardcoded to 1 and 0"), but the comment attributes it to `B-foliage-paint-ignores-align-to-normal-and-random-yaw` — the ticket whose scale half was carved OUT to `B-foliage-paint-hardcodes-scale-and-zoffset` by that ticket's `#3`. The concession is real; the id in it points at the wrong ticket. Recorded here rather than edited into either sibling, both of which are deliberately untouched.
- `#6-title-narrowed-rotation-measured-closed` `DONE` verifier — 2026-08-30, **13:32 build**, editor pid 18592. **Supersedes `#5` of this session, which left the ticket IN-REVIEW because the editor was down and rotation was a source reading only. It has now been measured, by me, on this build.** `#5`'s "unverified live" caveat is withdrawn; its citations stand.

  **Rotation, measured, with the control `#5` asked for.** Six instances painted through `foliage.paint` onto `LandscapeHeightfieldCollisionComponent_9` (`surface {preset:"landscape"}`, `surfaceTrust: "trusted"`), z 5000 projected down to 252.5–257.8 (`deltaZCm` ≈ -4745, `projected: true`, `projectedCount: 6`). All six came back with **non-zero pitch, yaw and roll** and `alignedToNormal: true`, `rotation.alignedCount: 6`: pitch/roll `-7.08/-0.71`, `-4.13/-6.22`, `-0.20/7.47`, `7.52/2.66`, `5.41/-6.32`, `4.86/6.96`, with yaw spread across `89.8, 137.4, 1.9, -66.2, -136.5, -40.4`. Read back through **`foliage.get_instances`**, a different verb, the same six rotators — so this is not the paint response describing its own intent. `#4` measured `pitch 0, yaw 0, roll 0` on all four of its instances; that is the finding inverted.

  **The control is what makes it conclusive, and it passes.** A second type created with `alignToNormal:false, randomYaw:false` and painted on the same terrain produced three instances at **`pitch 0, yaw 0, roll 0`** with `alignedToNormal: false`, while still projecting correctly (z 5000 → 225.0–234.3). So the verb reads the type's rotation contract in *both* directions rather than merely applying some rotation — which is the distinction `#5` named as the thing that had to be separated. Mechanism at HEAD `1a9e5778`, re-derived: `FoliageHandler.cpp:843-846` reads `RandomYaw` / `RandomPitchAngle` / `AlignToNormal` / `AlignMaxAngle` off the type, `:908-915` writes the rotation, `:975-979` calls `AlignToNormal(GroundNormal, TypeAlignMaxAngle)` against the seat's `Contact.AverageNormal`.

  **Title narrowed, and the exact change recorded because it is a scope decision.** `#4` withheld DONE on the title and named narrowing as the cheapest correct resolution while declining to make the call as a tester; `#5` declined it too. Making it here, now that nothing behind it is outstanding. **Removed:** "and no normal alignment" and "with a zero rotator and unit scale". **Kept:** the trace/projection subject, which `#4` verified and this entry re-verified. Both removed clauses have their own tickets and neither is being dropped — rotation is `B-foliage-paint-ignores-align-to-normal-and-random-yaw` (IN-REVIEW; its `#2` fix is what was measured above, so a tester there can cite this entry), scale/ZOffset is `B-foliage-paint-hardcodes-scale-and-zoffset` (OPEN, Medium). The `align-to-normal` tag is dropped for the same reason. Nothing in the body is edited: history is append-only and the body's rotation prose is now historical rather than wrong.

  **Scale is still hardcoded and that is deliberate, not an oversight in this close.** All nine instances above read `scaleX/scaleY/scaleZ = 1` through `foliage.get_instances`, so `B-foliage-paint-hardcodes-scale-and-zoffset` is confirmed live on this build. It is out of this ticket's narrowed scope by construction.

  **One citation correction, repeated from `#5` because it survives this close:** the in-source concession at `FoliageHandler.cpp:916-918` attributes the scale/ZOffset hardcoding to `B-foliage-paint-ignores-align-to-normal-and-random-yaw`, whose scale half was carved OUT to `B-foliage-paint-hardcodes-scale-and-zoffset`. The concession is correct; the ticket id in it points at the wrong ticket. Neither sibling file was edited from here.

  **Still not exercised, and not claimed:** the **unprojected** branch (`projectToGround:false` / no `surface`), which `#4` also flagged. Both this entry and `#4` graded only the projected path. If that branch turns out to skip rotation, it is a new finding against `B-foliage-paint-ignores-align-to-normal-and-random-yaw`, not a reason to reopen this ticket, whose narrowed subject is projection.
