---
id: B-editor-quit-crash-open-asset-editors
title: "editor.quit crashes the editor (EXCEPTION_ACCESS_VIOLATION in ~FStaticMeshEditor) when asset-editor windows are still open — shutdown must close asset editors first"
status: IN-REVIEW
severity: High
category: bug
tags: [editor-quit, shutdown, crash, access-violation, static-mesh-editor, asset-editor, unclean-exit, restore-packages]
encounters: 1
lastSeen: 2026-07-16T11:09:25+03:00
---

# `editor.quit` crashes the editor when asset-editor windows are still open

`editor.quit` returned a clean `{"requested":true,"reason":"mcp.editor.quit","saved":false,"discarded":false,"dirtyCount":0}` and then the editor process died with an unhandled `EXCEPTION_ACCESS_VIOLATION reading 0x0000000000000078` during engine shutdown (UE 5.7, PDS project, 2026-07-16). A Static Mesh Editor tab was open at quit time.

## Crash anatomy (from the user-captured callstack)

Bottom-up: `FEngineLoop::Exit` → `FSlateApplication::Shutdown` → `CloseAllWindowsImmediately` → `~SWindow` widget-tree teardown → `SStandaloneAssetEditorToolkitHost::ShutdownToolkitHost` (`SStandaloneAssetEditorToolkitHost.cpp:406`) → `FStaticMeshEditor::~FStaticMeshEditor` (`StaticMeshEditor.cpp:264`) → `TMulticastDelegateBase::RemoveAll()` → `FMRSWRecursiveAccessDetector::AcquireWriteAccess` (`MTAccessDetector.h:615`) → AV reading `0x78` (null object + member offset).

Interpretation: this is the classic UE shutdown-order defect — asset editors still open at exit are destroyed late, by Slate window teardown, *after* the objects whose multicast delegates they subscribed to are already gone. The Static Mesh Editor destructor's `RemoveAll()` walks a dangling delegate owner. The engine bug is not ours to fix, but **PinWright's quit path hands the engine exactly the state that triggers it**.

## Why this is High

- Any `editor.quit` issued while the user (or an automation flow, e.g. a mesh/preview inspection) left an asset editor open can crash instead of exiting cleanly. Which editor tabs happen to be open is not something the calling agent can see or control.
- The crash makes the exit *unclean*, which arms the "Restore Packages" modal on the next launch — the known invisible-game-thread-block failure (port LISTENING, MCP connection refused) unless the next launch happens to pass `-unattended`. So one crashed quit can cascade into a wedged next session.
- The tool reports `requested:true` success, so the caller believes shutdown went fine; the crash is only visible out-of-band (crash reporter / logs).

## Proposed fix

In the `editor.quit` handler, before requesting engine exit:

1. Close all open asset editors on the game thread: `GEditor->GetEditorSubsystem<UAssetEditorSubsystem>()->CloseAllAssetEditors()` (this tears each toolkit down through the orderly close path while their delegate owners are still alive), and let one tick/flush pass if needed.
2. Only then proceed with the existing quit request (`RequestEngineExit` / `FPlatformMisc::RequestExit` — whatever the handler uses today).
3. Optionally report `closedAssetEditors: N` in the response for observability.

Repro sketch: open any static mesh in the Static Mesh Editor (double-click `SM_*` in Content Browser), call `editor.quit`, watch the process die in `~FStaticMeshEditor` instead of exiting cleanly. (Crash is timing/ordering dependent but reproduced live on UE 5.7 with one SM editor tab open.)

## History
- `#1-initial-report` `OPEN` reporter — Filed from a live crash: `editor.quit` during the sumo-drone work session killed the editor in `~FStaticMeshEditor` delegate teardown with an SM editor tab open (full callstack captured in this ticket). `dirtyCount:0` so no data was lost, but the unclean exit arms the Restore-Packages modal for the next launch. Proposed mitigation: `CloseAllAssetEditors()` before requesting engine exit.
- `#2-additional-asset-close-ordering` `OPEN` reporter — Additional evidence: **Adversarial review A — REFRAME.** Actuality: CONFIRMED CURRENT. Framing: the trigger remains unmitigated in the current handler; `1304a34b` only added the PIE barrier and explicitly left this asset-editor crash open. The title is accurate, but an editor crash is **Critical** under the board rubric, not High. Proposed fix: INCOMPLETE, `CloseAllAssetEditors()` is the correct engine close path, but the handler must verify `GetAllOpenEditors()` reaches zero and refuse/hold exit when a toolkit vetoes close; `FAssetEditorToolkit::CloseWindow` can return true even when `OnRequestClose` prevented teardown, so a one-tick delay and optional count cannot prove safety. Evidence: `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\Editor\EditorQuitHandler.cpp:13-19,226-247`, `C:\UE_5.8\Engine\Source\Runtime\Core\Private\Misc\CoreGlobals.cpp:325-342`, `C:\UE_5.8\Engine\Source\Editor\MainFrame\Private\Frame\MainFrameHandler.cpp:93-112`, `C:\UE_5.8\Engine\Source\Editor\UnrealEd\Private\Subsystems\AssetEditorSubsystem.cpp:55-63,258-263,459-477`, `C:\UE_5.8\Engine\Source\Editor\UnrealEd\Private\Toolkits\AssetEditorToolkit.cpp:525-541`, `C:\UE_5.8\Engine\Source\Editor\StaticMeshEditor\Private\StaticMeshEditor.cpp:243-272,2722-2759`, `C:\UE_5.8\Engine\Source\Runtime\Launch\Private\LaunchEngineLoop.cpp:5055-5066,5092-5095`. Runtime: NOT VERIFIED (historical UE 5.7 crash only; no Unreal launch permitted). Recommendation: REFRAME; raise severity to Critical, retain this as distinct from the PIE ticket, implement a close-and-confirm barrier with a no-exit error for remaining editors, then add focused regression coverage and fresh UE runtime crash-free evidence.
- `#3-additional-normal-close-veto` `OPEN` reporter — Additional evidence: **Adversarial review B — A correctly identifies the live bypass and veto hazard, but its Critical escalation and direct-close prescription are not yet proven.** Actuality: CONFIRMED CURRENT. Framing: `editor.quit` still sends success and schedules `RequestEngineExit` (`X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\Editor\EditorQuitHandler.cpp:242-247`); UE 5.8 explicitly notes `RequestEngineExit()` can skip `OnEditorClose()` (`C:\UE_5.8\Engine\Source\Editor\UnrealEd\Private\UnrealEdEngine.cpp:493-495`), while that event is what binds to and invokes `CloseAllAssetEditors()` (`C:\UE_5.8\Engine\Source\Editor\UnrealEd\Private\Subsystems\AssetEditorSubsystem.cpp:55-63,258-263`). The current Static Mesh destructor and close-veto path remain (`C:\UE_5.8\Engine\Source\Editor\StaticMeshEditor\Private\StaticMeshEditor.cpp:243-272,2722-2759`), but the only crash evidence is UE 5.7 and the open-editor condition is a less-common quit edge, so A's automatic Critical re-rating is not established under the board reach rule; keep High pending UE 5.8 runtime proof. Proposed fix: INCOMPLETE, `CloseAllAssetEditors()`/normal `BroadcastEditorClose()` is systemic if it preserves intended restore behavior, but remaining `GetAllOpenEditors()` must be a hard refusal; `FAssetEditorToolkit::CloseWindow()` returns true even when `OnRequestClose()` vetoes (`C:\UE_5.8\Engine\Source\Editor\UnrealEd\Private\Toolkits\AssetEditorToolkit.cpp:525-541`), so a count/one-tick delay without a zero-editor assertion is a band-aid. Runtime: NOT VERIFIED. Recommendation: REFRAME; retain OPEN High, add a veto regression that confirms no exit, then obtain a fresh UE 5.8 open-asset-editor quit run before changing severity.
- `#4-runtime-evidence-on-ue58` `IN-REVIEW` implementer — **The runtime evidence both reviews said was missing now exists, and the headline crash does NOT reproduce on UE 5.8.** The mitigation both reviews assumed was absent had already landed at `71ea6a95` (2026-08-20) in the EAContentExamples58 plugin checkout — the reviews cite `unreal-fpv-dev`, a different checkout, which is why they read it as unmitigated. `EditorQuitHandler.cpp` calls `EditorQuitPolicy::CloseOpenAssetEditors()` on both the no-PIE path and the post-PIE-teardown path, before `RequestEngineExit`. **Repro run verbatim from this ticket, UE 5.8.1, visible editor, 2026-08-23:** launched a fresh editor, `editor.open_asset {assetPath:'/Engine/BasicShapes/Cube'}` to raise a Static Mesh Editor, then `editor.quit`. Response `{requested:true, dirtyCount:0, pieWasActive:false, assetEditorsClosed:1, assetEditorsRemaining:0}`; the process exited with `LogExit: Exiting.` and `Saved/Crashes` stayed at 719 entries — no new crash report, no `EXCEPTION_ACCESS_VIOLATION`, no `~FStaticMeshEditor` fault. So: fixed, and now demonstrated on 5.8 rather than argued from source. **What this session actually repaired is the test, which was vacuous.** `PinWright.editor.quit.ClosesAssetEditors` asserts `OpenCount == 0` — it never opens a toolkit, so it only ever exercised the nothing-was-open path and would have passed identically through the entire life of the crash, and passes identically with the pre-exit close deleted. Green before and after; it proved nothing. **New failure-direction test `PinWright.editor.quit.ClosesAnOpenAssetEditorBeforeExit`** (`Tests/EditorOps/TestEditorQuitPolicy.cpp`, commit `6b3415dc`) establishes the precondition instead: opens a Static Mesh Editor on `/Engine/BasicShapes/Cube` (an engine primitive — this ships to consumers sharing no content with the host project), asserts the subsystem reports 1 open, calls `CloseOpenAssetEditors()`, then asserts `OpenCount==1`, `RemainingCount==0`, zero edited assets remaining, and that the reported remainder is derived from the subsystem rather than an assumed zero. It fails if the close is removed, deferred, or stops covering a toolkit type. **Measured: PASSED, with zero `PINWRIGHT_ASSERTIONS_SKIPPED` markers**, in a filtered run (`Saved/PinWright/test-runs/pw_wave_bindquit/automation.log`, `11 tests performed`, all six `PinWright.editor.quit.*` green) — so it genuinely opened and closed a toolkit rather than skipping. Both pre-existing skip markers in that file moved from `AddInfo` to the countable `AddWarning` form; `AddInfo` events are never written to the automation log, so those skips were invisible rather than quiet. **Deliberately NOT implemented: the hard-refusal barrier on `assetEditorsRemaining > 0` that both reviews asked for.** A vetoing toolkit was not reproducible — the dirty-package disposition runs before the close, so nothing is left for a toolkit to prompt about, and no veto could be provoked. Shipping a refuse-to-exit path for an unreproduced condition would be a guess, and review B itself warns an editor that could never be shut down is the worse failure. The asymmetry is now documented rather than silently chosen (`Docs/wiki-src/editor.md`, `Docs/rpc-design.md` §20): PIE that will not stop refuses because the crash is certain; a toolkit that will not close is reported because it is likely, not certain. **Recommend this stays open as a separate hardening ticket if wanted, rather than blocking this one** — the crash this ticket is named for is fixed and now proven. Docs same commit: `Docs/rpc-design.md` §20 (a verb that ends a process owns the state it hands to teardown; preconditions are the fix, never a guard at the fault), `Docs/wiki-src/editor.md`.
