---
id: B-no-editor-identity-handshake
title: "No editor-identity handshake: RPCs silently execute on a co-located same-project editor"
status: IN-REVIEW
severity: High
category: bug
tags: [transport, http, multi-editor, misroute, identity]
encounters: 1
lastSeen: 2026-07-20T00:00:00Z
---
# No editor-identity handshake: RPCs silently execute on a co-located same-project editor

PinWright derives the MCP port per project path (auto-derive on: `19880 + hash(projectPath) % 10240` → 24281 here). Two editors of the **same** project derive the **same** port, so the second fails to bind. The refuse-to-serve fix from `B-port-conflict-false-active` makes that second editor log an error and not serve (`SocketHttpServer.cpp:221-233`; caller `PinWrightSubsystem.cpp:159-178` sets `bStreamingActive=false`, logs "Transport failed to bind port", and continues — the editor orphans as a live non-serving process). But the **first** editor keeps serving on the port, and the MCP client connects to it.

The gap: no RPC returns editor identity. The `system.*` namespace exposes job/inspect/console/live-coding/`run_*` handlers but nothing carrying pid, project path, launch args, or a caller-supplied token (searched `Handlers/System`, `Handlers/Environment`). So a caller that "launched its own editor" cannot tell from any response that its RPCs are being served by a **different** (e.g. the user's interactive) editor. A mutating RPC then hits an unintended live editor.

This is distinct from `B-port-conflict-false-active`: that ticket fixes the *second* (failing) editor's self-honesty; auto-derive does **not** help here (same project path → same port), and nothing lets a *client* verify which live editor answers it.

Session evidence: an agent launched a 2nd `UnrealEditor.exe` for this project while the user's editor served 24281. Log: `HTTP transport binding port 24281.` → `LogSocketHttp: Error: Port 24281 could not be bound ...` → `Transport failed to bind port 24281.` The 2nd editor orphaned (non-serving). The client's `blueprint.set_default` (`EyeMaterialTeam0` on `/App/HELIOS/Drones/Atlas/B_PioneerSumo`) ran on the FIRST editor, which reinstanced its live drones and crashed. No response field flagged the wrong target.

**Workaround:** Before any mutating call, confirm `call()` reaches YOUR editor (attribute a read RPC's result to the instance you launched, or confirm the target editor's log shows the bind you expect). If the per-project port is already served, STOP — do not launch a second same-project editor and drive it; it will not serve, and your RPCs hit the first.
**Fix:** Add an editor-identity RPC (e.g. `system.identity`) returning pid / project path / launch args (or a caller-supplied launch token) so a client can verify its target before the first mutating call; and/or have the failed-bind editor exit (or hard-disable MCP-dependent automation) rather than orphan as a live non-serving process.

## History
- `#1-initial-repro` `OPEN` reporter — Two same-project editors share a derived port (24281); the second refuses to serve (B-port-conflict-false-active fix), but no editor-identity RPC lets the client detect its RPCs are served by the FIRST editor. A misrouted `blueprint.set_default` reinstanced the user's live drones and crashed that editor; nothing in the response indicated the wrong target.
- `#2-additional-identity-worktree-gap` `OPEN` reporter — Additional evidence: **Adversarial review A — the worktree contains an opt-in identity RPC, but the delivered target is not yet proven.** Actuality: PARTIAL. Framing: The original “no RPC” claim is stale against current plugin source, but same-project endpoint misrouting remains unsafe by default because the proxy only probes liveness and does not require or verify identity before forwarding mutations. Proposed fix: INCOMPLETE, `system.identity` reports useful live fields and bounded retry is present, but the handler/test are untracked and coverage is direct registration/in-process capture rather than real `/mcp` or proxy behavior; no caller token or mutation gate exists. Evidence: `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\System\IdentityHandler.cpp:27` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\System\IdentityHandler.cpp:36` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Tests\Infra\TestSystemIdentityHandler.cpp:22` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Content\Python\mcp_proxy.py:1055` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Content\Python\mcp_proxy.py:1214` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Transport\PortBindRetry.cpp:67`. Runtime: NOT VERIFIED — no Unreal launch/build/test run under review. Recommendation: VERIFY FIRST; compile/load the current plugin, call `system.identity` through actual MCP/proxy with two same-project editor candidates, assert returned PID/project path/command line/httpPort matches the intended editor, and exercise the failed-bind/rebind path before closing or reframing around automatic identity enforcement.
- `#3-additional-identity-enforcement` `OPEN` reporter — Additional evidence: **Adversarial review B — A's “PARTIAL” label undercalls the still-open handshake defect.** Actuality: CONFIRMED CURRENT. Framing: “No RPC” is stale only for the untracked addition; the title and High severity remain accurate because generic proxy calls still trust whichever same-project process owns the port, and a silent mutating misroute can corrupt or crash the user's editor. Proposed fix: BAND-AID, `system.identity` is opt-in/manual (no caller nonce, expected PID/token, or mutation gate), while the 60-second same-port rebind expands the ownership race. Evidence: `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Content\Python\mcp_proxy.py:1055` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Content\Python\mcp_proxy.py:2236` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Handlers\System\IdentityHandler.cpp:27` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Transport\PortBindRetry.cpp:28` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Tests\Infra\TestSystemIdentityHandler.cpp:22` `X:\src\unreal\unreal-fpv-dev\Plugins\PinWright\Source\PinWright\Private\Tests\Transport\TestPortBindRetry.cpp:54`. Runtime: NOT VERIFIED. Recommendation: KEEP; make launch-time identity/token binding enforceable in the proxy and transport (including direct HTTP), reject mutations on mismatch, then run a two-editor same-project acceptance through real `/mcp` and proxy before closing.
- `#4-identity-handshake-and-dispatcher-gate` `IN-REVIEW` developer — Verified the gap reproduces in `X:\src\unreal\unreal-fpv-new` first: no `system.identity`, no `IdentityHandler.cpp`, no `_expect_editor`, and `UPinWrightSettings::DerivePortFromPath` (PinWrightSettings.cpp:80) still hashes only the project directory, so two same-project editors derive one port. Reviews #2/#3 were filed against `unreal-fpv-dev` and describe files that do not exist here. Added `Transport/EditorIdentity.h/.cpp`: identity MEASURED from the running process — pid, a per-boot GUID minted in-process (deliberately NOT `FApp::GetInstanceId()`, which `-InstanceId=` lets an outside launcher dictate), absolute project file, project name, engine version, `__DATE__`/`__TIME__` plugin build stamp, executable path, command line. Published by a new `system.identity` verb, which also reports measured `bound_http_port` beside requested `configured_http_port` (omitted rather than zeroed when unbound) and, for in-process callers on an orphaned editor, `contested_http_port` / `bind_attempts` / `bind_retry_exhausted`. Enforcement is a dispatcher gate, not an opt-in read: `_expect_editor` inside a call's `args` (beside `_format`/`wait`; it cannot be a sibling of `args` — McpRequestCore rejects any tools/call field but method/args/path) is checked as the FIRST thing in `FRpcDispatcher::ProcessRequest`, before the ready gate and before any deferral, so a mismatched call is refused with the new `EDITOR_IDENTITY_MISMATCH` and a payload carrying `expected` + `actual` for exactly the asserted fields, and the handler body never runs. An empty assertion, or one naming a field outside `pid`/`instance_id`/`project_file`/`project_name`/`engine_version`/`plugin_build`, is refused as INVALID_PARAMS rather than partially applied — a silently ignored assertion reads as a passed one. NOT made mandatory: a call with no `_expect_editor` behaves exactly as before, so no existing client breaks. On the failed-bind half: it is already observable here and needs no change — `SocketHttpServer.cpp:281` logs a full-remediation Error, `PinWrightSubsystem.cpp:337` logs "NOT serving", `GetServerStatus()` returns `PortInUse`, `PortAdvertisement::ReconcileWhileNotServing()` retracts the stale port file, and a bounded retry runs; the residual gap is that the orphan cannot answer over HTTP, which is why those fields are exposed to in-process callers. Regression test `Tests/Transport/TestEditorIdentity.cpp` (6 ids under `PinWright.transport.identity.*`, `check_test_ids.py` CLEAN at 4772 ids): measured fields match this process, an unasserted call and `system.identity` itself are served, a truthful assertion runs the handler, a wrong pid yields the typed refusal naming both sides with a call-count assertion proving the handler did NOT run, misspelled/empty assertions are refused, and a synthetic-identity pass pins pid spelling tolerance, path-case folding, and the same-project/different-instance case. Not compiled or run — the orchestrator builds. Wiki: new "Which editor am I talking to" section in `Docs/wiki-src/system.md`, placed above the first `###`.
