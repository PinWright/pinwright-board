---
id: F-sequencer-fbx-roundtrip
title: "Sequencer FBX import/export of animation"
status: IN-REVIEW
severity: Medium
category: feature
tags: [sequencer, fbx, import-export]
---

# Sequencer FBX import/export of animation

No FBX operations exist in the sequencer handlers (grep `Fbx|FBX` in `Source\PinWright\Private\Handlers\Sequencer\` = zero matches; ~55 `sequencer.*`/`sequence.*` RPCs, none FBX). Agents cannot round-trip sequence animation through external DCC tools (Maya/MotionBuilder) or ingest baked transform/property FBX onto sequence bindings. (Skeletal mocap is *partially* reachable today via `asset.import` -> `sequencer.add_animation_track`, but that is asset-reference-only and skeletal-only; there is no export path at all, and no baked transform/property-track import path.)

Engine API — present on THIS host's UE 5.7 **and identically on 5.3-5.6** (verified across all installed engines), so this is NOT a 5.8-only capability and needs NO version gate: `USequencerToolsFunctionLibrary::ExportLevelSequenceFBX` / `ImportLevelSequenceFBX` (module `SequencerScriptingEditor`, `SequencerTools.h`). This is the same canonical `SequencerTools` path Epic's Python FBX round-trip uses.

Proposed scope (signatures corrected to the real 5.7 API):
- `sequencer.export_fbx(path, filePath, bindings[]?)` — export bound-object animation to an `.fbx` file via `ExportLevelSequenceFBX(FSequencerExportFBXParams)`. NOTE: the engine exporter has **no** sub-range parameter (the ticket's original `range` arg does not exist in the API) — it always exports the whole playback range. `bindings[]` restricts which object bindings export; omit = all bindings.
- `sequencer.import_fbx(path, filePath, bindings[]?)` — import `.fbx` animation onto the sequence's object bindings via `ImportLevelSequenceFBX(World, Sequence, bindings[], UMovieSceneUserImportFBXSettings*, filename)` (baking transform + animated-property keys, matched to bindings by FBX node name). Import takes a bindings **array** + a typed settings object, NOT a single binding + free-form options.

Acceptance: `sequencer.export_fbx` on a keyed sequence writes a non-empty `.fbx` on disk and reports success; `sequencer.import_fbx` reads it back onto the bindings and reports success — both through the real `USequencerToolsFunctionLibrary` engine calls. Full transform-fidelity round-trip (clear keys -> re-import -> evaluated transform matches the original within tolerance) additionally holds when the bindings resolve to live world actors.

## History
- `#1-no-fbx-io` `OPEN` reporter — No FBX import/export in sequencer handlers (grep verified). Epic 5.8 ships FBX round-trip via SequencerTools; file export_fbx/import_fbx RPCs.
- `#2-reword-then-implement` `IN-REVIEW` fuzz2 — REWORD then implemented (GO). Gap confirmed present; engine FBX API (`ExportLevelSequenceFBX`/`ImportLevelSequenceFBX`) verified present + stable across UE 5.3-5.7 (all installs) so dropped the misleading `parity-ue58` framing — NO version gate. Corrected the Fix signatures: export = `FSequencerExportFBXParams` (no `range` param, full playback range only); import = `bindings[]` array + `UMovieSceneUserImportFBXSettings`. Shipped `sequencer.export_fbx` + `sequencer.import_fbx` over `USequencerToolsFunctionLibrary` in new `Handlers/Sequencer/SequencerFbxHandler.cpp`; added the `SequencerScriptingEditor` dep in `PinWright.Build.cs`. Regression test `PinWright.Sequencer.FbxRoundTrip.ExportImport` (`Tests/Sequencer/TestSequencerFbxRoundtrip.cpp`): create sequence -> key a Location.X ramp -> `export_fbx` writes a non-empty .fbx (13728 bytes observed) -> `import_fbx` reads it back; both report success. Compiles clean; test PASSES; differential verified (red pre-fix — handlers unregistered). NOTE: also fixed a pre-existing committed baseline break that blocked ALL plugin compilation — `Tests/Assets/TestAnimGraphHandlers.cpp` had two malformed `FResponseSink(lambda, 120.0)` calls (2-arg `TFunction` ctor + a missing `Initialize(` paren) left by the SSE-transport refactor (HEAD=origin/master=3dea7d6); corrected to the canonical 1-arg form.
- `#3-test-phase-fix` `IN-REVIEW` developer — The #2 "canonical 1-arg form" correction of `Tests/Assets/TestAnimGraphHandlers.cpp` compiled but left 13 `PinWright.anim.*` tests red at runtime (all `errorCode=UNKNOWN_ACTION`): the SSE refactor that collapsed `Transport->RegisterCompletion(sink, 120.0)` into `Initialize(FResponseSink(sink))` ALSO deleted the `Dispatcher.DrainAutoRegistrations(nullptr)` call that both fresh-dispatcher helpers (`DispatchAnimDecompileViaDispatcher` / `DispatchAnimRpcViaDispatcher`) need to register handlers into their local dispatcher — `Initialize` only installs the sink (verified `RpcDispatcher.cpp:242`), so with no drain every dispatch hit the no-handler path (`RpcDispatcher.cpp:515`). The stray `120.0` was `RegisterCompletion`'s timeout, correctly dropped. Restored `DrainAutoRegistrations(nullptr)` after `Initialize` in BOTH helpers (matching the canonical `Tests/Infra/DispatcherTestHelpers.h::MakeDispatcher` and the pre-refactor code the 3dea7d6 diff removed). No production/handler change — `SequencerFbxHandler.cpp`, `PinWright.Build.cs`, and the FbxRoundTrip test are unchanged; the reviewer's ESCALATED CORRECTNESS CONCERNS were none. Final full suite: 3623/3623 green, 0 crashes; `PinWright.Sequencer.FbxRoundTrip.ExportImport` asserted (13728-byte FBX round-trip, no skip).
